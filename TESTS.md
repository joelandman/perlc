# perlc — Test Defect Tracking

## Phase Tracking

| Phase | Status | Description |
|-------|--------|-------------|
| Phase 0 | **COMPLETE** | Establish correctness gates |
| Phase 1 | **IN PROGRESS** | Original D1-D16/B1 registry fully re-verified 2026-07-09 (most already fixed or stale/not-applicable — see Defect Registry). 26 new defects found in the same pass (D17-D44 numbering, some IDs retired/merged). Comprehensive re-review on 2026-07-14 re-verified every OPEN defect (15/17 still open, D44 now fixed, D49 partially improved) and found 16 new defects (D67-D82) via a docs-vs-source audit and a fresh corner-case probe; `tests/harness.sh` rerun confirmed zero regressions (same 7 pre-existing failures). **Fix-work session, same day**: worked the resulting top-10 correctness-fix priority list in order and fixed D66, D71, D72, D67, D73, D34, and D8a (REMEDIATION.md items 49-55), each with dedicated smoke+deep regression tests and a full harness re-run confirming zero regressions after every fix; three of them (D66, D71, D67) each surfaced one further, separate, still-open defect while writing their own regression test (D83, D84, D85 respectively), now folded into the priority list. **See CLAUDE.md's "Correctness state" banner for the current top-10 correctness-fix and top-10 missing-feature priority lists.** |

**Note**: Status is also tracked in `PLANS.md`. `REMEDIATION.md` tracks individual fixes with commit references.

---

## Test Infrastructure

### Test Runner: `run_tests.sh`

A comprehensive test runner that provides correctness validation for all test files by comparing compiled binary output against the Perl interpreter.

#### Key Features

- **Automatic correctness validation**: For every `.pl` test file, the runner:
  1. Compiles the file with `perlc`
  2. Runs the Perl interpreter on the original file
  3. Runs the compiled binary
  4. Compares outputs byte-for-byte
  5. Reports PASS/FAIL with diff details

- **Test classification**: Automatically classifies tests as:
  - **Assertion tests**: Have built-in `die()` checks (threads_atomic.pl, xs_ffi.pl, etc.)
  - **Smoke tests**: Print output but no assertions (hello.pl, arith.pl, etc.)
  - **Benchmark tests**: Performance-oriented (fibn.pl, mbs.pl, nbody.pl, fk.pl, bt.pl)
  - **Placeholder tests**: No real testing (xs_dbi_test.pl — skipped)

- **Benchmark result caching**: Long-running benchmark tests are cached per-hostname in `tests.csv`:
  - Before running a benchmark, checks if a cached result exists for the current hostname
  - If found, uses the cached result instead of re-running (saves time on CI/build servers)
  - Cache file format: `hostname,benchmark_test,time_seconds,success,output,accuracy`
  - Cache commands: `--show-cache` (view), `--clear-cache` (clear), `--force-benchmark` (re-run), `--no-benchmark-cache` (disable)
  - Default timeout for benchmarks: 300 seconds (configurable via `--benchmark-timeout`)

- **Flexible modes**:
  - `test-all`: Run all tests with perl output comparison
  - `test-smoke`: Run only smoke/benchmark tests
  - `test-assertion`: Run only assertion tests
  - `--smoke-only` / `--assertion-only`: Filter modes
  - `--skip-compile`: Only run perl interpreter (verify perl works)
  - `--skip-interp`: Only run compiled binary (verify compilation works)
  - `--sort-output`: Sort output lines before comparing (for non-deterministic tests)
  - `--ignore-whitespace`: Ignore trailing whitespace differences
  - `--ignore-exit`: Ignore exit code differences
  - `--verbose`: Show diff details for failures
  - `--dry-run`: Print what would be tested
  - `-j N`: Run N tests in parallel

- **Output status codes**:
  - `PASS`: Compiled output matches perl
  - `FAIL`: General failure
  - `SKIP`: Test skipped (placeholder, filter, etc.)
  - `COMP`: Compilation failed, perl passed
  - `RUNT`: Perl failed, compiled binary passed
  - `MISM`: Both ran but outputs differ
  - `TIME`: Timed out

- **Temporary files**: `/tmp/perlc_test_<name>_*/`
  - `perl_output.txt`, `compiled_output.txt`
  - `perl_stderr.txt`, `compiled_stderr.txt`
  - `diff.txt`, `status.txt`, `compile.log`

#### Usage

```bash
# Run all tests with perl output comparison
./run_tests.sh tests/

# Run with verbose output
./run_tests.sh -v tests/arith.pl

# Run in parallel
./run_tests.sh -j 4 tests/

# Only smoke tests
./run_tests.sh --smoke-only tests/

# Only assertion tests
./run_tests.sh --assertion-only tests/

# Dry run
./run_tests.sh -n tests/
```

#### Makefile Targets

```bash
make test        # Run assertion-based contract tests (fast)
make test-all    # Run all tests with perl output comparison
make test-smoke  # Run only smoke/benchmark tests
make test-assertion  # Run only assertion tests
```

### Test Quality Assessment (Pre-Runner)

Before the test runner was added:

| Category | Count | Description |
|----------|-------|-------------|
| Smoke tests (zero assertions) | 29 | Print output but never verify correctness |
| Light validation (conditional prints) | 9 | Use `print "ok" if $cond` but don't affect exit code |
| Proper assertions (die-on-fail) | 5 | Exit non-zero on failure |
| Placeholders (no-op) | 1 | Always passes |

**The test runner eliminates the smoke test problem by providing external correctness validation for all 29 smoke tests automatically.**

## Defect Registry

**Last full re-verification: 2026-07-09** — every entry below (D1-D16, B1) was empirically re-tested against the current source (not assumed from prior doc state); D17+ are newly discovered in that same pass, together with a 3-way parallel review (defect-registry re-verification, root-cause of the 13 `harness.sh` failures, and a broad probe for the "documented as working but silently wrong in one corner" failure pattern that produced the D1xx-series fixes committed today). Every CRITICAL item below with a repro was independently re-run and confirmed (not just trusted from the sub-agent report) before being recorded here.

**Second full re-verification: 2026-07-14** — a 4-way parallel review (every prior OPEN defect re-tested against HEAD `4ce6e88`; a fresh CLAUDE.md/README.md-vs-source audit; a broad new probe of string/numeric/list/regex/OOP corners not previously covered; and a full `tests/harness.sh` rerun) found: 15 of 17 previously-OPEN defects still reproduce exactly as documented; D44 (tie/untie STORE/FETCH) is now fixed with no dedicated commit or test; D49 (`%SIG`) improved from a hard parse error to parsing-but-still-nonfunctional; 16 new defects (D67-D82, see "Newly Discovered Defects" below); and zero regressions in the harness (same 7 pre-existing failures as before). `make test` (distinct from `tests/harness.sh`) currently **fails** due to D67 and a missing `syscall()` builtin, contradicting CLAUDE.md's top-banner claim — see the note after D82 below.

Status `FIXED` entries are in `REMEDIATION.md` with commit references.
Status `OPEN` entries are unresolved; severity tiers below double as the fix-priority order.

### Build Environment

| Defect ID | Problem | Status | Notes |
|-----------|---------|--------|-------|
| B1 | LLVM 18 + GCC 15/16 incompatibility — `__normal_iterator` incomplete type errors | **NOT-APPLICABLE** | `make clean && make` builds cleanly on this system with g++ 15.2.0 + LLVM 18 (the `force_complete_std.h` workaround already handles it). Separately: `CLAUDE.md`/`README.md` claim **LLVM 21**/clang-22 while the `Makefile` hardcodes `llvm-config-18`/`clang-18` and `main.cpp` links generated programs via `clang-18` — this is a **documentation bug**, not a build defect; the toolchain is and has been LLVM 18. |

### Critical Defects (fix first — crashes and silent wrong data)

| Defect ID | Problem | Status | Notes |
|-----------|---------|--------|-------|
| D38 | `my ($x, $y) = (10)` — list assignment with fewer RHS values than LHS scalars **segfaults** | **FIXED (2026-07-09)** | Root cause: a single-element parenthesized RHS with no comma parses down to a bare scalar node, not an `ArrayLit` (parser's grouping-parens simplification). The list-assignment codegen's `emitArrayPtr(RHS)` then returned null, and the fallback path passed the resulting `PerlValue*` directly to `perl_array_get_ref()` as if it were a `PerlArray*` — raw type confusion reading garbage memory. Fixed (codegen.cpp ~4264) by wrapping the lone scalar in a real one-element `PerlArray` first, matching the pattern already used by `@arr = RHS` and lvalue-slice assignment elsewhere in the same function (those were already correct; only this call site had the bug). Tests: `tests/list_assign_arity_smoke.pl`, `tests/list_assign_arity.pl` (16 sections), verified byte-for-byte against real Perl. |
| D39 | `my ($a, $b, @rest) = LIST` — trailing array does not collect remaining values | **FIXED (2026-07-10)** | Root cause: `parseMy`'s `my (LIST) = RHS` handling correctly tracked each LHS variable's sigil for its `my` declaration, but when building the *assignment-target* list it unconditionally called `makeScalar()` on every name — stripping the sigil, so `@rest` became a bare `ScalarVar` node named "rest" that codegen treated as a same-named (never-declared) scalar slot; the real `@rest` array stayed empty. Fixed in two places: parser.cpp now preserves the sigil (emits `ArrayVar`/`HashVar` for a trailing `@rest`/`%rest`), and codegen.cpp's list-assignment loop (~4266-4351) now recognizes an `ArrayVar`/`HashVar` LHS target and slurps every remaining RHS element into it via a new `perl_array_extend_from(dst,src,start)` runtime helper (plus `perl_hash_from_list` for the hash case), instead of doing a single per-index scalar assign. Also fixed a related pre-existing bug surfaced while testing: `perl_hash_from_list` silently dropped a trailing unpaired key (odd-length list) instead of assigning it `undef` (real Perl's behavior); now fixed to match. Tests: `tests/list_assign_rest_smoke.pl`, `tests/list_assign_rest.pl` (13 harness-compared sections + the odd-trailing-key case verified manually, since real Perl's accompanying stderr warning can't be suppressed — no `no warnings 'misc'`/`%SIG` support — see the test file's comment), verified byte-for-byte against real Perl. |
| D37 | `foreach`/loop-variable aliasing is entirely absent — mutating `$_` or the loop var does not write back to the source array | **FIXED (2026-07-09)** | Root cause: the general (non-integer-range) `foreach` codegen allocated one stable `PerlValue*` cell for the loop var before the loop, then each iteration cloned the current element (`perl_array_get`, which calls `perl_clone`) and copied that clone's value into the stable cell via `perl_assign` — the loop var was always a private copy. Fixed (codegen.cpp ~3506-3538) by borrowing the array's own element pointer each iteration (`perl_array_get_ref`, no clone) and storing that pointer into the loop var's alloca every iteration instead of copying a value into a fixed cell; any mutation now writes through to the array's own cell. Existing per-iteration closure-capture isolation (`foreach my $x (@a) { push @subs, sub { $x } }`) is unaffected — it already relied on `perl_array_push_capture` cloning the value at capture time, not on loop-var cell identity, so it stays correct with either implementation. **Not fixed as part of this**, logged as a new low-priority gap: real Perl raises "Modification of a read-only value attempted" when the loop var aliases a literal-list element (`foreach my $x (1,2,3) { $x *= 2 }`); perlc now silently allows the mutation instead (discarded along with the temporary list, so not a correctness risk — just a missing diagnostic). Tests: `tests/foreach_aliasing_smoke.pl`, `tests/foreach_aliasing.pl` (12 sections: `$_`/named-var/string/nested/hashref-element aliasing, next/last interaction, empty/single-element arrays, closure isolation, post-loop growth, sequential loops), verified byte-for-byte against real Perl. Also TSan-clean (`threads.pl`/`threads_atomic.pl`/`destroy.pl` use `foreach` heavily). |
| D40 | 3+ level chained hash/array autovivification silently fails on both read and write | **FIXED (2026-07-10)** | Root cause: the `ArrowDeref`-assignment codegen (`$ref->[i]=val`/`$ref->{k}=val`, with autoviv) only special-cased a base that was exactly one `HashElem`/`ArrayElem` level (`$h{k}[i]=val`, `$a[i]{k}=val`). A base that was itself another `ArrowDeref` — exactly what a 3+ level chain produces (`$h{a}{b}{c}` parses as `ArrowDeref(ArrowDeref(HashElem(h,a),"b"),"c")`) — fell through to the generic non-autovivifying fallback (`emitExpr` + `perl_deref_hash`/`perl_deref_array`), which silently produced a fresh, disconnected, immediately-discarded container when the middle level didn't exist yet. Fixed with a new recursive `emitAutovivContainer()` helper (codegen.cpp) that walks an arbitrary-depth `HashElem`/`ArrayElem`/`ArrowDeref` chain, autovivifying every missing intermediate level via the existing `perl_(hash\|array)_autoviv_(hash\|array)[_sv]` runtime primitives. **Scoping fix mid-implementation**: the first version of this fix intercepted *any* `ArrowDeref` base, which broke `nb.pl`/`nbody.pl` (segfault) — a scalar-ref-rooted chain like `$bodies->[0][3]` was already correctly handled by a pre-existing FLAT_ARRAY-aware fallback, and the `perl_array_autoviv_array`/`perl_hash_autoviv_hash` runtime helpers only recognize the `PERL_REF_ARRAY`/`PERL_REF_HASH` tags — routing a FLAT_ARRAY-tagged inner array through them silently destroyed it. Fixed by adding an `isElemRootedChain()` check so the new recursive path only activates for chains genuinely rooted in a `%hash`/`@array` element; a scalar/ref-rooted chain keeps using the original FLAT_ARRAY-aware fallback untouched. **Separate, still-open gap found and NOT fixed here** (logged as **D50**): `$ref->{a}{b} = val` starting from an *existing* scalar ref (e.g. `my $ref = {}; $ref->{a}{b}=1;`) still silently fails — that's the plain-deref fallback branch (scalar/ref-rooted, not element-rooted), a different code path than what this fix touches. Tests: `tests/autoviv_chain_smoke.pl`, `tests/autoviv_chain.pl` (14 sections: 2-5 level chains, mixed hash/array nesting in all orderings, sibling-branch independence, missing-path reads, `ref()` checks, and an explicit `nb.pl`-style FLAT_ARRAY regression guard), verified byte-for-byte against real Perl. |
| D22 | `sort` silently returns an empty list for any argument shape beyond its few special-cased forms (`sort grep{...}@arr`, `sort map{...}@x`, `sort some_func()`) | **FIXED (2026-07-10)** | Root cause: the parser's `sort` handling (parser.cpp ~2031-2091) only recognized `sort keys/values %h`, `sort @arr`, `sort (LIST)`, `sort qw(...)` — anything else (grep{}/map{}/reverse/function-call results, nested sort chains) fell through with `elems` never populated, silently becoming `sort()`. Fixed by adding a final fallback: parse any other argument as a single general expression into `n->left` (the same slot already used for `sort keys %h`/`sort @arr`) — no codegen changes needed, since `emitArrayPtr()` already has cases for `GrepFunc`/`MapFunc`/`Call`/`ReverseFunc`/etc. that correctly turn any of those into a `PerlArray*`. **Scoping note**: `sort BAREWORD(args)` (e.g. `sort get_nums()`) is deliberately NOT covered — real Perl itself treats a bareword immediately followed by `(` in this position as the `sort SUBNAME LIST` comparator form (see D42), a separate, genuinely ambiguous grammar case, not "call it and sort the result" — confirmed by testing directly against real Perl, which gives surprising (non-call) output for that exact shape too. Tests: `tests/sort_list_expr_smoke.pl`, `tests/sort_list_expr.pl` (12 sections: grep/map/reverse arguments, custom comparator + grep, nested sort/map chains, inline in join/print, empty-result case, regressions for the pre-existing forms), verified byte-for-byte against real Perl. |
| D42 | `sort SUBNAME LIST` (named comparator sub, no braces) is broken — silently empty, or a hard parse error inside a nested call | **FIXED (2026-07-10)** | Root cause: the parser had no notion of a bareword sort-comparator name at all — `sort by_name @words` matched none of the recognized argument shapes. Fixed with two changes: parser.cpp now detects a leading bareword `IDENT` (not immediately followed by `,`/`=>`/`;`/EOF) as SUBNAME, consumes it, and lets the existing keys/values/@arr/(list)/qw/general-expression dispatch (from the D22 fix) parse whatever follows as the LIST. codegen.cpp gained a new `"subname"` sort mode: a comparator wrapper function (same shape as `sort { BLOCK }`) that assigns its two args into file-scope global `$a`/`$b` cells (lazily created if missing) and calls the named sub with no arguments — required because a separately-compiled named sub can only see `$a`/`$b` as real Perl package globals, not as the wrapper's own local allocas the way an inlined block comparator can. **Disambiguation confirmed directly against real Perl** (not guessed): `sort BAREWORD(...)` is SUBNAME + a parenthesized LIST, not "call the bareword as a function" — `sort get_nums()` sorts an *empty* list in real Perl (SUBNAME "get_nums" + the empty list `()`), while `sort get_nums(9,9)` sorts `(9,9)` using `get_nums` as comparator; both behaviors are now reproduced exactly. Tests: `tests/sort_subname_smoke.pl`, `tests/sort_subname.pl` (11 sections: ascending/descending string/numeric comparators, parenthesized and empty-parenthesized lists, `keys %h`, empty/single-element arrays, regressions for block comparators/default sort/D22's grep-sort), verified byte-for-byte against real Perl. |
| D23 | Regex literals collapse `\\` (escaped backslash) into a single `\`, silently changing match semantics | **FIXED (2026-07-10)** | Root cause: the lexer's `readRegex()` (lexer.cpp ~159-169) special-cased `\\` in the source and collapsed it to a single `\` in the pattern string, while passing every *other* backslash-escaped pair (`\d`, `\s`, `\/`, etc.) through unchanged — inconsistent and wrong. PCRE2 needs to see `\\` exactly as written (two characters) to interpret it as "one literal backslash"; collapsing it first turned the next character into part of an unintended escape (`/a\\d/`, literal backslash then "d", silently became `/a\d/`, the digit metaclass). Fixed by removing the special case — every backslash-escaped pair is now passed through unchanged, matching the general branch's existing (correct) behavior; PCRE2 does its own escape interpretation, and tolerates a redundant escape of the delimiter (`\/`) fine. `s///`'s lexer (`readSubst`) never had this bug — it already passed everything through unconditionally. **Separate, unrelated bug found while testing** (logged as **D51**, not fixed here): `\$` inside a *double-quoted string* (not a regex pattern) doesn't produce a literal `$` correctly — `"price: \$100"` prints `"price: 00"` under perlc instead of `"price: $100"`. Tests: `tests/regex_backslash_smoke.pl`, `tests/regex_backslash.pl` (10 sections: literal-backslash matching, `\\d` vs `\d` disambiguation, multiple consecutive backslashes, escaped delimiter, captures with backslash separators, substitution, `split`, escaped `$`), verified byte-for-byte against real Perl. |
| D25 | `return` inside a nested `eval{}` with no enclosing named sub compiles to `perl_die`, corrupting `$@` and control flow | **FIXED (2026-07-11)** | Root cause: `case NK::Return` assumed "if we're in `main`, there's no enclosing sub, so treat `return` as die" — wrong: real Perl's `return` inside `eval{}` always exits just the nearest enclosing eval block with the given value (confirmed directly against real Perl, including the non-obvious case where the eval is itself inside a named sub — the return targets the eval, not the sub, and execution continues in the sub afterward). Fixed with a new `evalReturnTargets_` stack (codegen.h/.cpp): `NK::EvalBlock` pushes `{resultAlloca, endBB}` before compiling its body and pops it after; `NK::Return` checks this stack first — if non-empty, stores the value into the innermost active eval's `resultAlloca` and branches to its `endBB` (a normal branch, not die's longjmp) instead of always dying. `NK::AnonSub` saves/clears/restores the stack around its own body compilation (matching how it already isolates `scopes_` etc.), since `return` inside an anonymous sub must target the anon sub, not an enclosing eval. **A separate, more fundamental, pre-existing bug was found and fixed while testing this** (confirmed unrelated to this fix — reproduces with zero `return` statements involved): `NK::EvalBlock`'s own codegen, when a *nested* eval was a non-last statement in the outer eval's body (`eval { my $x = eval {...}; MORE_CODE }`), left the block where `MORE_CODE` was compiled without ever storing the outer eval's actual computed result into its own `resultAlloca` before branching to its own `endBB` — silently discarding the real value and always reading back `undef`. Fixed by adding the missing store. This fix also resolves `tests/eval_exception.pl`'s pre-existing failure as a side effect. Tests: `tests/eval_return_smoke.pl`, `tests/eval_return.pl` (11 sections: catch-then-return, return-in-eval-within-sub, nested-eval targeting, anon-sub isolation, implicit-last-expr regression, no-value return, multiple sequential evals, die-after-return propagation, loop nesting, the nested-eval-no-return CFG bug, if/else branches), verified byte-for-byte against real Perl via the regular (non-TSan) `perlc`, including `threads.pl`/`threads_atomic.pl` passing their own internal self-checks in the same `tests/harness.sh` run. **`make test-tsan` could not be used to verify this fix directly**: the special TSan-*instrumented compiler binary* (`perlc_tsan`, built with `-fsanitize=thread` on the compiler's own C++ source, used only for this one Makefile target) hangs when compiling `tests/threads.pl` specifically with this fix applied (confirmed via `wchan=futex_do_wait` — blocked on a futex, not busy-looping), root-caused to a TSan+`fork()` interaction in `perlc`'s own subprocess invocation of `clang-18` for linking; `TSAN_OPTIONS=die_after_fork=0` gets past the actual hang (the output binary is produced successfully) but the compiler process is then very slow to exit (plausibly TSan's own shutdown/report phase). Bisected directly against the immediately prior commit (before this fix, same file, same environment): that build compiles `threads.pl` in ~3.6s with no hang at all, so this fix's changes do trigger the difference somehow — but `tests/threads.pl` uses no `eval` whatsoever, so the actual *logic* touched by this fix (`NK::Return`/`NK::EvalBlock`/`NK::AnonSub`) is never exercised by that file's compiled output; the correctness of this fix is separately and thoroughly verified via `tests/harness.sh` (57/69 passing, zero regressions, including `threads.pl`/`threads_atomic.pl` passing their own internal thread-safety self-checks when compiled with the regular, non-TSan `perlc`). Logged as **D54** for separate follow-up — likely a subtle change in the compiler binary's memory layout/timing (from the added code) newly exposing a latent, probabilistic TSan+fork race in the *compiler's own* subprocess handling, not a bug in this fix's logic or a thread-safety issue in generated programs. |
| D35 | `Carp::croak`/`confess` call `exit(1)` directly, bypassing `eval` entirely — cannot be caught, kills the whole process | **FIXED (2026-07-10)** | Root cause: `perl_carp_croak()` (runtime.c ~5144) called `fprintf(stderr,...)` then `exit(1)` directly, completely bypassing the die/eval/longjmp mechanism. Fixed by routing it through the existing `perl_die()` instead, which already had the correct dual behavior (longjmp to the nearest eval if one is active, or print+exit(1) at top level if not) — croak just needed to reuse it. `codegen.cpp` already routes both `croak` and `confess` through `perl_carp_croak`, so both are fixed by this one change. **Found and fixed as a direct dependency**: `perl_assign()` segfaulted on self-assignment (`$x = $x`) — freed `dst`'s old payload before reading/cloning from `src`, and when `dst==src` (as happens with the standard `eval {...}; die $@ if $@;` re-throw idiom, since `perl_get_dollar_at()` returns the live global's address, not a copy) that free invalidated `src` too, crashing on the subsequent `strdup(NULL)`. This was blocking the test suite for this very fix. Fixed by making `dst==src` a guaranteed no-op in `perl_assign`, plus the identical bug found in `perl_array_set`/`perl_hash_set_str`/`perl_hash_set_sv` (same free-before-clone-from-same-pointer pattern, causing silently wrong garbage values rather than a crash for array/hash-element self-assignment, e.g. `$a[0]=$a[0]`/`$h{k}=$h{k}`) — same fix pattern applied to all three. Two narrower, separate gaps found and NOT fixed (logged as **D52**, **D53**): `$@ = "..."` is a silent no-op (`$@` isn't wired up as an assignment target in `emitLValue` at all); and `my $r = \$x; $r = $r;` (self-assignment of a reference to a block-scoped lexical) gives the wrong dereferenced value when done inside a nested `{ }` block (works fine at file/top scope) — a narrower, separate self-assignment path from the ones fixed here. Tests: `tests/carp_croak_eval_smoke.pl`/`tests/carp_croak_eval.pl` (croak/confess catchability, nested eval, deep call chains, rethrow) and `tests/self_assign_smoke.pl`/`tests/self_assign.pl` (scalar/array-element/hash-element/global/array-ref self-assignment), verified byte-for-byte against real Perl. Also TSan-clean. |
| D36 | Any unrecognized bareword call without parentheses (`foo "hello";`) is silently parsed as two no-op statements — zero diagnostics | **OPEN** | This is *why* `croak "msg"` / `carp "msg"` (idiomatic, no-parens Carp style) silently vanish — croak/carp aren't lexer keywords, so `croak "boom";` parses as bareword `croak` followed by a discarded string literal, with no call emitted and no error raised. Broader than Carp: any bareword-call-without-parens to an unrecognized name is swallowed silently. |
| D9 | `floatSqrtOf_` cache (Stage 30: `v*v → x` rewrite for values assigned from `sqrt()`) is keyed only by variable name and never cleared, leaking across unrelated subs | **FIXED (2026-07-12)** | Confirmed with `sub f1 { my $a = sqrt(4); return $a } sub f2 { my $a = 10.0; return $a*$a }` — `f2()` returned `4` instead of `100`; the stale sqrt-input from `f1`'s `$a` bled into `f2`'s unrelated `$a*$a` because the cache (`floatSqrtOf_`, codegen.cpp) is keyed purely by variable name with no scope/function awareness, and was never invalidated when the same name got a later, unrelated `my` declaration. The stale reference happened not to crash only because the cached values are always `ConstantFP` LLVM nodes (safely referenceable across functions); the *value* was still numerically wrong for the referencing function's own variable. Fixed with a one-line addition: `floatSqrtOf_.erase(nm)` unconditionally before the existing conditional re-population, every time a `my $var = <float-RHS>` declaration is compiled — a fresh declaration always invalidates any previous association for that name (whether from an earlier declaration in the *same* function, i.e. shadowing, or a completely unrelated sub reusing the same name), and only re-establishes a new one when *this* declaration's own right-hand side was actually `sqrt(x)`. Verified: the original cross-sub repro, the same bug with call order reversed, same-function shadowing, the legitimate `sqrt(x)` optimization itself (still correctly recovers the exact input via `$d*$d`, using perfect-square test inputs since `sqrt(2)*sqrt(2)` isn't bitwise `2.0` — a floating-point precision fact, not a bug), multiple unrelated subs each legitimately using `sqrt` on the same variable name, and interleaved/repeated calls. `nb.pl` (the FLOAT_PAIR/sqrt-optimization-heavy benchmark this optimization was originally written for) still compiles and runs correctly with no observable performance regression. Tests: `tests/sqrt_cache_leak_smoke.pl` (2 checks) and `tests/sqrt_cache_leak.pl` (13 checks across 6 sections) — verified byte-for-byte against real Perl. |
| D57 | A directly self-recursive sub matching the AST-level inliner's shape (`my (@params) = @_; return expr`) hangs/crashes the compiler at compile time (LLVM crash / stack exhaustion), not just at runtime | **FIXED (2026-07-12)** | Root cause: `tryEmitInline()` (codegen.cpp) unconditionally inlines any call whose name is in `inlineSubs_`, with no self-reference/cycle guard; when the body being inlined contains a call to *itself* (or, transitively, to another inlinable sub that calls back to it — mutual recursion), the recursive `tryEmitInline()` call operates on the *same, unchanging* AST node every time, so the recursion never terminates — unbounded static recursion in the compiler's own C++ call stack, not a runtime loop, observed to crash with an LLVM "please submit a bug report" stack dump. Fixed with a two-phase approach in `compile()`: inlineable-shaped subs are first collected into a `candidates` map (not yet live for `tryEmitInline()`); a new `collectCalls()` helper finds every `NK::Call` inside each candidate's body expression, building a call graph restricted to edges between candidates (a call to a non-candidate sub can't be part of a cycle, since it just becomes an ordinary bounded call); a new `dfsFindCycles()` helper runs standard 3-color DFS cycle detection over that graph (a 1-node self-loop naturally covers direct recursion, longer cycles cover mutual recursion of any length); only candidates *not* found in a cycle become live entries in `inlineSubs_`. A cyclic sub still compiles and runs correctly — it just isn't inlined at its own recursive call site, falling back to an ordinary (safe) function call there. Verified: direct self-recursion (factorial, and fibonacci with *two* recursive calls in one return expression), 2-cycle and 3-cycle mutual recursion, and — as a regression check — that a genuinely acyclic chain of inlinable subs (A calls B, B doesn't call back to A) still gets inlined and still works correctly; `nb.pl`/`mbs.pl` (the two benchmarks CLAUDE.md documents as relying heavily on this inliner for their reported speedups) both still pass the harness with no performance regression observed. This fix also removes the need for `tests/nested_block_sub.pl`'s D57 workaround (a self-recursive sub matching the exact inlinable shape no longer hangs the compiler), though that test file's 3-statement-body version was left as-is since it's still valid, orthogonal coverage. Tests: `tests/recursive_inline_smoke.pl` (1 check: direct self-recursion compiles and runs correctly — this test's mere ability to produce output at all is itself a regression guard, since the pre-fix compiler never got that far) and `tests/recursive_inline.pl` (14 checks across 6 sections: direct self-recursion at multiple depths, fibonacci's two-recursive-calls-per-expression shape, 2-cycle and 3-cycle mutual recursion including wraparound, and two regressions for non-cyclic inlining) — verified byte-for-byte against real Perl. Full regression suite improved from 73/81 to 75/83 (2 new test files, remaining pre-existing failures unchanged, no new regressions). |

### High Severity Defects

| Defect ID | Problem | Status | Notes |
|-----------|---------|--------|-------|
| D24 | `do FILE` / runtime `require` of a dynamic file is a **non-functional no-op stub** — never actually parses or executes the target | **FIXED (2026-07-12)** | `do FILE` (only — `perl_runtime_require()`'s stub, used only when `require`'s argument isn't a compile-time-known literal, is untouched and separately out of scope; the common literal-argument case of `require`/`use` was already fully functional via compile-time inlining, unaffected by this stub either way) now has real semantics, implemented without a JIT: `perl_do_file()` re-invokes the `perlc` compiler binary itself as a subprocess (its own absolute path baked into every compiled executable at link time via a new `-DPERLC_SELF_PATH` define) with a new `--do-lib` mode that compiles the target file into a shared library instead of an executable, exposing a fixed-name entry point (`__perlc_do_run`, matching the same `PerlSubFnCtx` calling convention as an ordinary sub) that runs the file's top-level statements and returns the value of the last one (`CodeGen::compile()` gained an `asDoLib` parameter branching the "emit main()" section into this alternate entry-point shape, reusing `emitBlockLast()` — the same mechanism a sub body already uses — instead of `emitBlock()`+discard). Critically, the `--do-lib` shared library does **not** link its own copy of `runtime.c` — its `perl_*` symbol references stay undefined and resolve at `dlopen()` time against the *loading* process's own already-linked runtime (which now links with `-rdynamic` to expose those symbols), so a do'd file's registered package-qualified subs, `$@`, the PV allocator, and all other runtime state are genuinely shared with the caller rather than an isolated second copy — this is what makes a do'd file's subs (e.g. `TestDoModule::test_function()`) callable from the loading program afterward, via the *existing* `perl_call_named_sub()`/`perl_register_method()` string-keyed dispatch table already used for OOP method calls (do-lib subs are registered into that same table exactly like a normal program's `Package::name` subs already are). `perl_do_file()` wraps the entry-point call in the same `setjmp`/`s_eval_stack` mechanism `eval {}` already uses (`perl_die()` already had the correct dual "longjmp if an eval is active" behavior, so a `die()` inside the do'd file's top-level code is caught exactly like it would be inside an `eval {}` block, with no codegen changes needed for that part), and treats a nonzero exit / captured stderr from the compile-subprocess step as a syntax-error case (undef + `$@` set) exactly like a runtime failure. Loaded libraries are tracked and `dlclose()`d at `perl_cleanup()` (process exit) so their subs stay callable for the process's lifetime; temp `.so`/stderr-capture files are cleaned up immediately after use (safe to `unlink()` a `.so` right after a successful `dlopen()` on Linux — the inode stays mapped). Verified directly against real Perl: return value semantics, calling a loaded sub (with and without arguments), missing file, syntax error in the do'd file, runtime `die()` inside the do'd file (both the `undef` return and the `$@` message), and — beyond the test suite's own checks — nested `do` (a do'd file itself calling `do` on another file, exercising that the dynamically resolved symbols chain correctly through more than one `dlopen()` level). **Separate, narrower gap found and NOT fixed here** (logged as new **D58**): `our $pkgvar` and package-qualified sub redefinition do not persist/update correctly across *repeated* `do` calls on the *same* file within one process — each `do` call compiles an independent, disconnected shared library, so a package-global's backing storage isn't actually the single, persistent, process-wide slot real Perl's `our` implies; not exercised by `tests/test_do_filename.pl` (which never re-`do`s the same file) and confirmed unrelated to any of its 8 checks, all of which now pass. Tests: the pre-existing `tests/test_do_filename.pl` (already part of the canonical suite, `Makefile`'s `ASSERT_TESTS`, and `tests/harness.sh`'s `SELF_CHECKING` list — no new dedicated test files needed, since this file already exercised exactly the right 8 checks) now passes all 8 checks, byte-for-byte identical to real Perl. Full regression suite improved from 78/86 to 79/86 (one test newly passing, remaining pre-existing failures unchanged, no new regressions; confirmed the one other `make test`-only failure, `xs_ffi.pl`, is pre-existing and unrelated by reproducing it identically with this fix's changes stashed out). |
| D44 | `tie`/`untie`: `TIESCALAR`/`STORE`/`FETCH` interception does not happen — tied variables behave as plain scalars | **FIXED (verified 2026-07-14)** | Originally: verified directly with a `Doubler` tie class (`STORE` doubles the value): real Perl printed `x=10`, perlc printed `x=5` — `STORE`/`FETCH` were never actually invoked on read/write, only `TIESCALAR` ran (at `tie` time). Re-verified 2026-07-14 during the comprehensive review pass with the identical `Doubler` repro: perlc now prints `x=20`, byte-identical to real Perl — `STORE`/`FETCH` interception now works. No commit in the visible git log is explicitly titled for this fix; it appears to have been resolved as a side effect of other work between the last verification and now. No dedicated regression test exists yet — add one (`tests/tie_store_fetch.pl`) before trusting this stays fixed. |
| D38a | `s/pat/repl/` with alternate delimiters (`s{pat}{repl}`, `s#pat#repl#`, etc.) is not lexed at all | **OPEN** | `readSubst()` (lexer.cpp) is only invoked when the token is literally `s/`; every other delimiter falls through to identifier parsing and produces "Expected /regex/ or s/// or tr/// after =~". A common Perl style (especially for patterns that contain `/`) is entirely unsupported. |
| D38b | `s/$/text/` (or other empty/anchor-only matches) **segfaults** | **FIXED (2026-07-09)** | Root cause: `perl_regex_subst`'s zero-length-match handling (runtime.c ~4441) copied `s[pos]` (the old search-start) instead of `s[mstart]` (the actual match position) — wrong for an anchor like `$` that matches ahead of `pos` (at end-of-string). This left `pos = mstart+1 = slen+1` after a match exactly at end-of-string, and the following `size_t rem = slen - pos` (both unsigned) underflowed to ~SIZE_MAX, driving a multi-exabyte `memcpy`/`realloc` that corrupted the heap — the segfault surfaced later, inside `pcre2_match_data_free()`, once the corruption reached PCRE2's own bookkeeping. This is what the original registry's vague "npos underflow" description was actually pointing at. Fixed: use `mstart` for the zero-width-match char copy, and clamp the final `rem` computation so it can't underflow. Tests: `tests/regex_subst_zero_width_smoke.pl`, `tests/regex_subst_zero_width.pl` (17 sections: end/start anchors × global/non-global, empty/single-char subjects, lookahead, empty-pattern-every-position, `/mg`, embedded newlines, return-value-is-count, array-element targets), verified byte-for-byte against real Perl. |
| D38c | `s///e` does not evaluate the replacement as Perl code — just interpolates captures literally | **OPEN** | `s/(\d)\+(\d)/$1+$2/e` gives `"2+3"` under perlc instead of real Perl's `"5"`. `CLAUDE.md` explicitly and repeatedly claims `/e` "evaluates replacement as Perl expression" — it does not; it behaves identically to `/e`-less substitution. |
| D8a | ``EXPR or return VALUE`` parses successfully but does not actually return — falls through as a dead value-producing expression | **FIXED (2026-07-14)** | Semantic bug, not a parse error: `f() or return "X";` compiled, but execution continued to the next statement instead of returning; real Perl returns early with `"X"`. Root cause: `EXPR or return VALUE` parses into a `BinOp("||")` whose RHS is a `Block` wrapping a single `Return` statement (`Parser::parseOrRhs`, needed since `return` is statement-shaped, not an ordinary expression). `CodeGen::emitBlockLast` (which handles any `Block` used in expression context) has a special case for a block whose *last* statement is `Return`: it captures the return's value without emitting a real return, on the assumption that some outer caller — e.g. the code that emits a sub or `do`-file body right after calling `emitBlockLast` on it — will perform the actual return afterward using that captured value. That assumption is correct for a sub/do-file body, but the `||`/`&&` `BinOp` codegen has no such outer caller: it just fed the captured value into the "or"'s own result and continued executing, so the return silently never happened at all. **Fixed** with a new `CodeGen::emitShortCircuitRhs()` helper used by both the `&&` and `||` short-circuit codegen paths in place of a plain `emitExpr()` call on the RHS: it detects this exact "Block wrapping a single Return" shape and dispatches straight to `emitStmt()` on the `Return` node — invoking the real, complete Return-handling logic (the `evalReturnTargets_` check, so `return` inside `eval{}` reached via `or return` still only exits the eval as real Perl requires; local-restore; scope cleanup; and the actual `ret`/branch instruction) — instead of routing through `emitBlockLast`'s capture-only shortcut. `emitBlockLast` itself was deliberately left untouched, since many other callers (ordinary sub bodies, `do`-file bodies, ternary/if branches) correctly rely on its existing behavior. Also checked `last`/`next`/`redo`/`die`/`warn` in the same `EXPR or STMT` position — none share this bug: `last`/`next`/`redo` are handled by dedicated `emitStmt` cases that already emit a real branch regardless of which code path invokes them, and `die` (via `perl_die`, marked LLVM `NoReturn`) genuinely never returns control to the caller at runtime even though the IR technically continues past its call site, so its behavior is correct in practice despite the unrelated IR-shape quirk. Verified directly against real Perl: `or return`/`and return` guard clauses (single and sequential/multiple), `or return;` with no value, a plain trailing return and a mid-body return (regression, confirming the untouched `emitBlockLast` path), `or return` inside `eval{}` (confirming it still exits just the eval, not the enclosing sub — the trickiest interaction to get right), `or return` inside an anonymous sub, and plain `||`/`&&` (the symbolic forms, which never go through `parseOrRhs` and are structurally unaffected). Tests: `tests/d8a_or_return_smoke.pl` (2 checks) and `tests/d8a_or_return.pl` (15+ checks across 9 sections) — verified byte-for-byte against real Perl. Full `tests/harness.sh` regression suite re-run after the fix with zero new failures. |
| D8b | `EXPR or printf(...)` fails to parse | **OPEN** | `parseOrRhs`'s keyword whitelist (parser.cpp:933-953) covers `return/die/warn/last/next/redo/print/say/push/unshift` but omits `printf`, producing a hard parse error. |
| D12 | `wantarray` context is not propagated into `print`/`printf` sub-call arguments | **FIXED (2026-07-14)** | Confirmed: `print ctx(), "\n"` called `ctx()` in scalar context under perlc regardless of `print`'s actual (list) context; real Perl correctly propagates list context. Root cause: `print`/`say`/`printf`'s codegen evaluated each argument via a plain `emitExpr()` call with no regard for `callCtx_` (the compiler's existing flag for "the expression about to be evaluated is in list context, so a nested sub call should see `wantarray()==true`") — every other list-context call site (list assignment, lvalue slices, etc.) already set `callCtx_ = 1` around its RHS; print/printf's argument evaluation never did. **Fixed** by setting `callCtx_ = 1` around each print/printf argument's evaluation, at all four sites (print-to-filehandle, print-to-stdout single-arg, print-to-stdout multi-arg, printf's format+args). **Two subtleties found and fixed while implementing/testing this**: (1) `callCtx_` is a *one-shot* flag — the `Call` node's own codegen reads it once and immediately resets it to `0`, so it must be set fresh before *each* argument in a multi-argument print/printf, not just once before the whole argument list (setting it once before a loop of N arguments only actually reached the first call — confirmed directly: `print "a=",ctx()," b=",ctx(),"\n"` initially still showed the second `ctx()` call seeing scalar context). (2) A first version of the fix set `callCtx_ = 1` unconditionally before evaluating each *entire* argument expression, which caused two real regressions in the pre-existing test suite (`wantarray.pl`, `newfeatures.pl`): `say test_wantarray() eq 'scalar';` — `test_wantarray()` is nested *inside* an `eq` comparison, and real Perl's comparison/arithmetic/string operators always force scalar context on their own operands regardless of the outer expression's context, but blindly setting `callCtx_=1` before evaluating the whole `BinOp("eq", ...)` leaked list context straight through `eq` into the nested call. Fixed by adding `CodeGen::isCallLikeForContext()`, which only permits setting `callCtx_ = 1` when the argument's own top-level AST node is itself call-like (`Call`/`MethodCall`/`CallCodeRef`) — this correctly handles the common, directly-reported case (`print ctx(), ...`) without leaking context through an enclosing operator. **New, separate, pre-existing defects found while investigating this** (not introduced by this fix, confirmed to reproduce identically without it — logged as D87 and D88, not fixed here): D87, `wantarray()` never returns `undef` for genuine void context (a bare statement-level call) — the compiler's wantarray model is architecturally binary (scalar/list only), with no third void state anywhere; and D88, the same "`callCtx_` leaks through scalar-forcing operators" issue this fix specifically guards against for print/printf still exists, unfixed, at every *other* `callCtx_` call site (e.g. `my @a = (ctx() eq "x")` still incorrectly gives `ctx()` list context) — fixing that generally would require auditing every comparison/arithmetic BinOp codegen site, a broad change out of scope for this narrower, print/printf-specific fix. Verified directly against real Perl: single and multi-argument `print`/`printf`/`say`, filehandle (`STDOUT` and scalar-variable) forms, nested sub calls, and regressions for genuine scalar-assignment and list-assignment context. Tests: `tests/d12_print_wantarray_smoke.pl` (2 checks) and `tests/d12_print_wantarray.pl` (13+ checks across 6 sections) — both use a side-effect-recording technique (`ctx()` pushes what `wantarray()` actually saw onto an array) rather than capturing `print`'s own stdout output, since perlc doesn't support in-memory filehandles (`open($fh,'>',\$scalar)` — confirmed separately, not logged as its own defect since it's a documented, pre-existing "Not Yet Implemented" gap, not a regression). Also found and logged while writing these tests, NOT fixed here: D86, `print {$fh} LIST` (the brace-block filehandle form) is misparsed as a hash-literal argument — the plain `print $fh LIST` form used in these tests works correctly. Full `tests/harness.sh` regression suite re-run after the fix (and again after the regression fix) with zero net new failures. |
| D34 | `defined EXPR` without surrounding parens fails to parse (`defined $x`, not just `defined($x)`) | **FIXED (2026-07-14)** | One of the single most common Perl idioms; a bare `if (defined $x)` was a hard parse error under perlc. Root cause: the parser's `defined` handling unconditionally called `consume(TK::LPAREN, "(")` right after the keyword, with no optional-parens support at all — unlike neighboring named unary operators (`ref`, `abs`, `int`, `sqrt`, `exists`), which already made their parens optional via `match(TK::LPAREN)`. Fixed the same way, but with one deliberate improvement over those neighbors: rather than always calling the full `parseExpr()` for the bare argument (which those neighbors do, and which would incorrectly over-consume a trailing `&&`/`||`/comparison as part of the argument), the no-parens case now calls `parseShift()` — the precedence level that correctly matches real Perl's actual "named unary operator" precedence (tighter than comparisons/equality, looser than shift/additive/multiplicative) within this parser's own precedence chain (`parseCmp → parseBinding → parseShift → parseAdd → parseMul`). An explicit paren still parses the full expression inside, since parens always override precedence. Verified directly against real Perl: basic defined/undef checks, the parenthesized form (regression), `if`/`unless`/`return unless` idioms, precedence against `&&`/`||`/`+`/`<` (including a case that unambiguously distinguishes `(defined $x) && $y` from the wrong `defined($x && $y)` grouping — a defined-but-falsy `$y=0` gives different results, `0` vs `1`, under the two readings), and `defined` on hash/array elements and arrow-dereferenced fields. Tests: `tests/d34_defined_noparens_smoke.pl` (2 checks) and `tests/d34_defined_noparens.pl` (15+ checks across 6 sections) — the deep test omits `use warnings` (an intentional `$undef + 1` precedence check triggers real Perl's harmless "Use of uninitialized value" warning, D56/D48, same precedent as other files in this suite). Full `tests/harness.sh` regression suite re-run after the fix with zero new failures. |
| D41 | `local $h{key}` / `local $arr[idx]` (element-level `local`) not supported | **OPEN** | Parse error. `local` on a whole array/hash/scalar works (D3, fixed); `local` on one element of a hash/array does not. |

### Medium Severity Defects

| Defect ID | Problem | Status | Notes |
|-----------|---------|--------|-------|
| D26 | `use constant` from an inlined module is exposed as a global sub, ignoring package scoping / `@EXPORT` | **FIXED (2026-07-11)** | Root cause: `inlineModules()` (main.cpp) textually rewrote any `use constant NAME => VAL` found anywhere in the recursively-inlined token stream — including inside `.pm` files pulled in via `use`/`require` — into a plain global `sub NAME { return VAL; }`, regardless of which package declared it or whether the module actually exported it. A module's purely-internal constant (never added to `@EXPORT`/`@EXPORT_OK`) silently became callable, unqualified, from any importer. Fixed by threading an `isMainScript` flag (true only for the outermost call over the user's own script) and an `explicitImportNames` list (the `qw(...)` from the `use Module qw(...)` call site that pulled in the module currently being scanned) through `inlineModules()`. For constants found with `isMainScript` false, the fix now always emits a fully-qualified sub (`Package::NAME`, tracking `package NAME;` statements as it scans) so explicit qualification always works, and *additionally* emits the unqualified/bareword-global sub only when the name is actually visible: present in `explicitImportNames` when an explicit list was given (replacing the default export set, not adding to it — matching Exporter), otherwise present in the module's own `@EXPORT` (never `@EXPORT_OK` by default, matching real Perl). The main script's own top-level `use constant` (`isMainScript` true) is unchanged. This fix also resolved `tests/fileops.pl`'s pre-existing harness failure: it references `MathOps.pm`'s `PI`/`MAX` constants as bare names, neither of which is in that module's `@EXPORT`/`@EXPORT_OK` — real Perl (no `use strict` in that file) falls back to treating the unresolved barewords as literal strings (`"PI"`/`"MAX"`), which perlc's old leaking behavior didn't replicate (it printed the numeric values instead); `fileops.pl`'s stale comments claiming the numeric values were updated to match. Tests: `tests/d26_const_scoping_smoke.pl` (3 checks) and `tests/d26_const_scoping.pl` (10 checks across 6 sections: default-@EXPORT access, private-constant non-leak for both single and hash-form `use constant`, explicit qualified access to both private and exported constants, explicit `qw(...)` import of an `@EXPORT_OK` name, and main-script-constant regression), using two new fixture modules `tests/lib/D26Const.pm` and `tests/lib/D26ConstOk.pm` — verified byte-for-byte against real Perl. Full regression suite improved from 60/71 to 65/75 (2 pairs of new test files added, `fileops.pl` newly passing, remaining pre-existing failures unchanged, no new regressions). **Exporter-validation gap: FIXED (2026-07-12)**. `use Module qw(NAME)` now validates that `NAME` actually appears in the union of the module's `@EXPORT`/`@EXPORT_OK` (scanned via the pre-existing `scanExports()` helper), throwing a compile-time error matching real Perl's Exporter exactly — `"NAME" is not exported by the Module module` (confirmed byte-identical against real Perl's own error text; perlc's compiler process then exits 1, analogous to real Perl's "BEGIN failed--compilation aborted"). Added at *both* places an explicit import list gets processed: the first-load path (inside `inlineModules()`'s `for (dir : searchDirs)` loop, where `exports` was already being computed) and the previously-unvalidated already-loaded path (a second `use Module qw(...)` of a module `use`d earlier in the same file with a different list — that branch didn't recompute `exports` at all, so it needed its own fix: re-locate and re-scan the file there too). Constant names (from `use constant` inside the module) aren't found by `scanExports()` at all — it only recognizes literal `our @EXPORT[_OK] = ...` arrays — so they're deliberately exempted from this validation via `constMap` (which D26's own fix already populates only for actually-visible constant names), avoiding false-positive rejection of a valid constant import. Verified directly against real Perl: a bogus import name is correctly rejected with the exact matching error text (both at first-load and at already-loaded re-import), while every existing valid-import pattern in the test suite (`tests/d26_const_scoping.pl`, `tests/fileops.pl`, and the new tests below) continues to compile and run identically to before. Tests: `tests/exporter_validation_smoke.pl` (1 check) and `tests/exporter_validation.pl` (3 checks across 2 sections: explicit import of a default-`@EXPORT` name, and explicit import of two `@EXPORT_OK` names in one `qw()` list — which also exercises the already-loaded re-import path, since `MathOps` was already loaded by the first section's `use`) — verified byte-for-byte against real Perl. **The negative (rejection) path could not be added as an automated regression test**: `tests/harness.sh` always treats a compile failure as a test failure, with no "expected to fail compilation" category, so a test file containing a genuinely-bad import could never itself compile and run to report a result; verified manually instead (exact commands/output below). Full regression suite improved from 81/88 to 83/90 (2 new test files added and both passing; remaining pre-existing failures unchanged; the important result is *zero new regressions* among all pre-existing valid imports, since this fix only rejects invalid ones). |

Manual verification of the negative (rejection) path — `perl bad.pl` and `perlc bad.pl` compared directly, `bad.pl` containing `use lib "tests/lib"; use MathOps qw(nonexistent_function);`:
```
$ perl bad.pl
"nonexistent_function" is not exported by the MathOps module
Can't continue after import errors at bad.pl line 2.
BEGIN failed--compilation aborted at bad.pl line 2.
$ echo $?
255

$ ./perlc bad.pl -o /tmp/bad_out
Error: "nonexistent_function" is not exported by the MathOps module
$ echo $?
1
```
Both reject the import with matching error text (perlc's is a subset/paraphrase of Perl's multi-line message, since perlc doesn't model `BEGIN`-time failure propagation the same way — a single, clear "Error: ..." line and a nonzero compiler exit code is the correct analog for a static, ahead-of-time compiler). Also verified the already-loaded re-import path rejects a bad name identically (`use MathOps; use MathOps qw(nonexistent_function2);`).
| D27 | `system()` returns the plain exit code instead of Perl's shifted wait-status word | **FIXED (2026-07-11)** | Root cause: `perl_system()` already unwrapped the raw `wait(2)` status via `WEXITSTATUS()` before returning it, instead of returning the raw status word real Perl's `system()` returns. Real Perl's documented idiom for recovering the plain exit code, `$rc >> 8`, silently computed nonsense with the pre-unwrapped value (right-shifting an already-plain 0-255 code by 8 gives 0 almost always). Fixed by returning the raw `ret` from the underlying `system(3)` call directly (still returning `-1` as-is when `system(3)` itself fails to launch a child, matching Perl). This fix also resolves `tests/features.pl`'s pre-existing harness failure. **Two separate, narrower gaps found while testing, not fixed** (not logged as new IDs — low-priority, noted in the new test file's header instead): perlc's `system()` always shells out via `/bin/sh -c COMMAND`, while real Perl optimizes simple single-word commands with no shell metacharacters by exec'ing them directly and returning `-1` if the exec itself fails (perlc instead gets a shell-reported "not found", exit 127, for a nonexistent command); and the multi-argument LIST form of `system()` (`system($prog, @args)`, which bypasses the shell in real Perl) isn't accepted by perlc's parser at all (single-string argument only). Tests: `tests/system_exit_code_smoke.pl`, `tests/system_exit_code.pl` (5 sections: shifted exit code for failing/succeeding commands, shell-specific exit codes, raw-status-nonzero/zero checks), verified byte-for-byte against real Perl. |
| D28 | `sort`/`reduce` comparator blocks always rebind `$a`/`$b` to fresh locals, ignoring an outer lexical `my $a`/`my $b` that should shadow them | **FIXED (2026-07-13)** | Narrow: only triggers on the well-known "my $a used in sort comparison" Perl footgun (real Perl warns and produces a broken/non-monotonic sort in this case; perlc instead produced the "sensible" result, which was a *behavioral* divergence even though it looked like an improvement). Root cause: real Perl's `sort`/`reduce` comparator `$a`/`$b` are dynamically-aliased *package* variables, not a fresh lexical scope the construct introduces itself — an earlier `my $a`/`my $b` in an enclosing lexical scope permanently shadows the package variable's name for all later code in that scope, including a comparator block compiled after it (Perl resolves `$a`/`$b` via ordinary compile-time lexical scoping like any other variable). perlc instead always declared its own fresh `$a`/`$b` locals for the comparator, unconditionally winning any lookup. Fixed at both sites that bind `$a`/`$b` for a comparator: `reduce`'s block (compiled inline in the same function, since it's just a loop — codegen.cpp, `NK::Call` "reduce" handling) now uses the fully general `lookupVar()` and skips its own declaration when an outer scope already has `$a`/`$b` visible; sort's comparator (`case NK::SortFunc`, `mode=="custom"`) is compiled as a genuinely separate LLVM function (`cmpFn`, needed since it's passed as a real C function pointer to the underlying `qsort`-based sort routine) with a full scope reset, so its check can only reach a *file-scope* shadow (via `fileScalarGlobals_`, which that reset doesn't touch) — an enclosing *sub-scoped* `my $a` shadowing a sort comparator remains a narrower, separate gap (see D61). Verified `reduce`'s fix is exactly correct and algorithm-independent (fully deterministic): `tier1.pl`'s `reduce=5` now matches real Perl byte-for-byte. Verified sort's fix via an algorithm-independent technique (`push`-ing every value the comparator actually reads for `$a` into an outer array, rather than checking final sort order): every individual invocation correctly reads the shadowed value. **Deliberately not fully verified via final sort order**: once `$a` is shadowed to a fixed, non-varying value, the resulting comparator is not a valid strict-weak-ordering (violates antisymmetry), which is undefined behavior for *any* sorting algorithm — real Perl's `sort` is a stable mergesort, perlc's uses C's `qsort()` (unstable, pivot-choice-dependent), so a degenerate/non-transitive comparator can legitimately produce different final permutations (and even a different number of comparator invocations) between the two, independent of this fix; this pre-existing algorithm-choice divergence is out of scope here (also the reason `tier1.pl`'s `sort_len`/`sort_desc` lines remain in the harness's pre-existing-failure list even after this fix). **New defect found and logged, not fixed here**: D61 (sort's comparator has zero closure-capture support for outer *block*-scoped, as opposed to file-scope, lexicals — a separate, broader limitation than this fix's narrower `$a`/`$b` file-scope-only check). Tests: `tests/sort_reduce_shadow_smoke.pl` (1 check) and `tests/sort_reduce_shadow.pl` (9 checks across 7 sections: `$a`-only/`$b`-only/both shadowed via `reduce`, `reduce` no-shadow regression, sort no-shadow regression for both plain numeric and derived-key comparators, and the `push`-based per-invocation shadow-observation technique for `sort`) — verified byte-for-byte against real Perl. Full regression suite: 91/98 pass (up from 89/96 — 2 new test files added and both passing, the same 7 pre-existing failures unchanged including `tier1.pl`'s now-narrower `sort_len`/`sort_desc` divergence, no new regressions). |
| D29 | `sort` in scalar context returns element count instead of `undef` | **FIXED (2026-07-11)** | Four codegen sites treated `SortFunc` identically to `GrepFunc`/`MapFunc` for scalar-context list-producer handling, always returning `perl_array_len(av)` (the count): `emitExpr`'s direct case for `SortFunc`/`MapFunc`/`GrepFunc` (codegen.cpp ~5477, hit by e.g. `my $x = sort @a;`), `emitBlockLast`'s implicit-last-expression path (~2687), `emitBlockLast`'s captured-`return` path (~2711), and `case NK::Return:` (~3665) — the latter three only reachable when the enclosing sub's context is dynamically wantarray-dependent (`currentSubNeedsWantarray_`, i.e. the sub is called from both list and scalar context call sites). Real Perl's `sort` returns `undef` in scalar context (confirmed against perl 5.40.1) — unlike grep/map, which do return a count — and doesn't even evaluate its list argument or comparator block in that context. Fixed by splitting `SortFunc` out of the shared handling at all four sites: the first (unconditionally-scalar) site now returns `perlUndef()` directly without evaluating anything; the three wantarray-dependent sites now select `perlUndef()` for the scalar branch specifically for `SortFunc`, while grep/map keep `perl_array_len()`. **Accepted, narrower divergence from real Perl** (documented in the test file, not fixed — would require real control-flow branching instead of a `select` to skip evaluation entirely): the three wantarray-dependent sites still eagerly evaluate the sort list/block to produce a value for both branches of the `select`, whereas real Perl skips evaluating sort's arguments entirely in scalar context; this only affects side-effect timing in the rare dynamic-wantarray case, never the returned value. Tests: `tests/sort_scalar_context_smoke.pl` (3 checks), `tests/sort_scalar_context.pl` (9 sections covering plain/block/named-sub comparators, boolean context, all 3 wantarray-dependent sites in both list and scalar call context, and grep/map regression) — named subs are deliberately declared at file scope rather than nested in blocks, to avoid the pre-existing, unrelated D45 bug. Verified byte-for-byte against real Perl (both new files deliberately omit `use warnings` — real Perl's harmless "Useless use of sort in scalar context" warning would otherwise land on stderr and break the exact-match harness comparison). Full regression suite: 62/73 pass (up from 60/71 — 2 new test files added, the same 11 pre-existing failures, no new regressions). Fixing this also improved (but did not fully resolve) the pre-existing `wantarray_extended.pl` harness mismatch: that file's `count_sort()` sub returns `sort {...} @data` from dynamic-wantarray-dependent context and was previously printing perlc's wrong count value where real Perl prints an empty (undef-interpolated) string; it now prints the same matching empty string, leaving only an unrelated, pre-existing gap (D56, perlc doesn't emit Perl's "Use of uninitialized value" warnings) as the sole remaining diff. |
| D30 | `Time::HiRes` not implemented | **FIXED (2026-07-12)** | Implemented as a built-in (no real `Time/HiRes.pm` file), matching how `POSIX`/`Scalar::Util`/`Carp` already work in perlc: `time`, `sleep`, `usleep`, `gettimeofday` (list and scalar context), `tv_interval`. The one architectural wrinkle specific to this module: `time`/`sleep` are perlc *lexer keywords* with pre-existing, different (integer-only) builtin behavior — unlike `POSIX::floor` etc., which were never keywords in perlc or real Perl. Confirmed against real Perl that overriding them must be opt-in: a bare `use Time::HiRes;` with no import list does NOT override `time()`/`sleep()`; only `use Time::HiRes qw(time)`/`qw(sleep)` does. Implemented by threading the explicit `qw(...)` import list into `main.cpp`'s `importMap` (a new `Time::HiRes` special-case in `inlineModules()`, populating `importMap[name] = "Time::HiRes::" + name` only for explicitly-requested names, no default-export fallback) and having the parser consult `importMap_` specifically at its `KW_TIME`/`KW_SLEEP` sites (emitting a qualified `Time::HiRes::time`/`Time::HiRes::sleep` `Call` node instead of the normal `TimeFunc`/`SleepFunc` node only when explicitly imported). `gettimeofday`/`usleep`/`tv_interval` have no pre-existing keyword meaning, so — like `POSIX::floor` — they're always available unqualified with no import gating needed; `gettimeofday`'s list-context form (`(seconds, microseconds)`) required a new codegen case in `emitArrayPtr` (the generic user-sub `Call` handling there only recognizes real LLVM-declared subs, which this builtin-dispatch name never is). Runtime side: 6 new functions in `runtime.c`/`runtime.h` using `clock_gettime(CLOCK_REALTIME, ...)`/`nanosleep()` for sub-second precision. **Known, accepted, pre-existing divergence, not introduced by this fix**: `Time::HiRes::NAME()` (fully qualified) works in perlc even when `Time::HiRes` was never `use`d at all, whereas real Perl errors (`Undefined subroutine`) in that case — confirmed this is the *same* pre-existing behavior `POSIX::floor()`/etc. already have without a corresponding `use POSIX;`, a systemic characteristic of perlc's built-in-module architecture, not specific to this fix. Tests: `tests/time_hires_smoke.pl` (3 checks), `tests/time_hires.pl` (12 checks across 6 sections: hires `time()`, `gettimeofday` list/scalar context, `usleep`, `sleep` with a fractional argument, `tv_interval` two-arg and single-arg forms, fully-qualified access), and `tests/time_hires_no_import_regression.pl` (2 checks: `time()`/`sleep()` stay integer-resolution when Time::HiRes is never imported) — all verified byte-for-byte against real Perl. Every check verifies a *structural* property (decimal point present, element count, sign) rather than an exact timing value, so results are stable despite real wall-clock timing differing between runs. |
| D14 | Duplicated `cmpOps` array in two places in parser.cpp | **FIXED (2026-07-11)** | Code-quality only, not a correctness bug — no behavior change. Extracted the shared `print`/`say`/`printf` filehandle-detection logic (deciding whether a `$var IDENT` sequence is a comparison/word operator, meaning `$var` is a value, not a filehandle) into a single file-scope `isCmpOpWord()` helper near the top of parser.cpp, replacing the two verbatim-duplicated inline arrays+loops in `parsePrint()` and the `KW_PRINTF` case of `parseStmt()`. Verified zero behavior change: full regression suite unchanged at 65/75 (same pass/fail set as immediately before this change). |
| D56 | perlc never emits Perl's "Use of uninitialized value ... in concatenation/string" runtime warnings | **OPEN** | Found while fixing D29 — `tests/wantarray_extended.pl` interpolates a now-correctly-`undef` scalar into a string; real Perl emits a stderr warning (under `use warnings`, which that file has) that perlc doesn't replicate. Cosmetic/stderr-only (the actual computed/printed value already matches real Perl); low priority since implementing the general "uninitialized value" warning machinery is a broad, cross-cutting feature (every string/numeric op would need an undef-source check), not a targeted bug fix. |
| D45 | Named `sub` declared *inside* a bare `{ }` block that follows another bare block resolves to the wrong value at its call site | **FIXED (2026-07-11)** | Root cause was broader than the original repro suggested: **any** named sub nested inside **any** block-shaped construct — a bare `{ }` block (even a single one, with nothing preceding it), an `if`/`while`/`for` body, or an `eval{}` block, at any nesting depth — was affected, not just "≥2 preceding sibling blocks". `CodeGen::compile()` collected sub definitions for forward-declaration/emission (`subs_`) by scanning only `program.args`' direct top-level children for `NK::SubDef`, completely missing anything nested one level deeper. Since `emitStmt`'s `case NK::SubDef:` is a documented no-op ("bodies already emitted in compile()"), a sub missing from `subs_` never got its LLVM function body emitted *at all* — calling it silently fell through to a not-found fallback and returned an empty/undef value without the sub's body ever running (confirmed: adding a `print` as the first statement of such a sub's body never printed, proving the body genuinely never executed, not just "returned the wrong value"). Real Perl always compile-time-hoists a *named* sub declaration to package scope regardless of block nesting (unlike `my` variables, which really are block-scoped) — even a sub inside a never-executed `if (0) {...}` branch is still globally callable, confirmed directly. Fixed by replacing the direct-children-only scan with a recursive `collectSubDefs()` (codegen.cpp, placed just before `compile()`) that walks every child field a `Node` can hold (`left`, `right`, `cond`, `body`, `init`, `step`, `args`, `branches`) — mirroring the existing `hasWantarrayOrUserCall()` traversal — so `subs_` now contains every named sub in the program regardless of nesting depth. Deliberately recurses into a found `SubDef`'s own body too (a named sub nested inside another named sub, or inside an `NK::AnonSub` closure body, is unusual but real Perl still hoists it) — both fall out naturally from the generic walk with no special-casing needed. **Separate, unrelated, CRITICAL bug found and NOT fixed here** (logged as new **D57**): a directly self-recursive sub matching the AST-level inliner's shape (`my (@p)=@_; return expr`) hangs/crashes the *compiler itself* at compile time — reproduces at plain file scope with zero block nesting, so unrelated to this fix's root cause; `tryEmitInline()` has no self-reference guard and recurses into itself unboundedly when a call's own inlined body contains another call to the same sub. Worked around in this fix's own regression test by using a 3-statement sub body (which doesn't match the inliner's narrow 2-statement pattern) for the recursive-sub-in-a-block test case. Tests: `tests/nested_block_sub_smoke.pl` (2 checks) and `tests/nested_block_sub.pl` (13 checks across 13 sections: single/sibling-preceded/doubly-preceded bare blocks, call-after-block-closes, two-blocks-deep nesting, taken and untaken `if` branches, `for`/`while`/`eval` bodies, a recursive sub, and regressions for closures and plain file-scope subs) — verified byte-for-byte against real Perl. Full regression suite improved from 71/79 to 73/81 (2 new test files, remaining pre-existing failures unchanged, no new regressions). |
| D46 | Ternary with no space before `:` fails to parse when the true-branch is a bare scalar var: `$x?$y:"str"` | **OPEN** | Found while writing regression tests for D38. `(defined($x) ? $x : "nope")` parses fine; `(defined($x)?$x:"nope")` (no spaces) gives "Parse error: expected : but got 'nope'" — looks like a lexer tokenization issue where `$y:` with no space is mis-scanned. Minor/easily worked around (use spaces), but worth fixing since terse ternaries without spaces are common style. |
| D48 | `no PRAGMA ...;` (e.g. `no warnings 'misc';`) is a hard parse error, not silently ignored like `use PRAGMA;` | **OPEN** | Found while writing regression tests for D39 — needed to suppress real Perl's "Odd number of elements in hash assignment" warning for a deliberately-odd test input, but perlc doesn't parse the `no` form of a pragma statement at all (`use` pragmas are silently accepted/ignored per existing docs; `no` isn't handled the same way). Low priority (moot until a warnings system exists to have anything to suppress), but worth a one-line parser fix (treat `no IDENT ...;` the same inert way as unrecognized `use IDENT ...;`). |
| D49 | `%SIG` is not implemented — `$SIG{__WARN__} = sub {...}` is a hard parse error | **OPEN (partially improved, re-verified 2026-07-14)** | Found alongside D48 while looking for an alternative way to suppress a warning in a test. Not in `CLAUDE.md`'s feature list at all (signals generally aren't implemented, consistent with "Not Yet Implemented: ... signals"), so this is an expected gap, not a regression — logged here mainly as a cross-reference since it was hit in the same investigation. **Re-verified 2026-07-14**: the hard parse error is gone — `$SIG{__WARN__} = sub {...}` now parses and compiles cleanly as a plain hash assignment (`%SIG` is just an ordinary hash, no special recognition). But the feature itself is still non-functional: `ref($SIG{__WARN__})` returns empty instead of `CODE`, and `warn()` does not invoke the handler at all — perlc's `warn()` unconditionally prints straight to stderr. Net: no longer a compile-time blocker, but `%SIG` remains functionally unimplemented. |
| D50 | `$ref->{a}{b} = val` (chained autoviv starting from an *existing* scalar ref, not a hash/array element) silently fails | **PARTIALLY FIXED (2026-07-12)** | Found while fixing D40. `my $ref = {}; $ref->{a}{b} = 1; print $ref->{a}{b};` printed empty under perlc (real Perl: `1`). Distinct code path from D40: the base of the outer `ArrowDeref` here is another `ArrowDeref` rooted in a bare scalar variable, not a `HashElem`/`ArrayElem`, so it took the plain-deref fallback (`emitExpr` + `perl_deref_hash`, no autoviv) — the intermediate `$ref->{a}` read back `undef`, and dereferencing `undef` as a hash produced a fresh, disconnected, immediately-discarded temporary the write to `{b}` was silently lost into. **Fixed for all-hash-key chains** (any depth): added `isScalarRootedAllHashChain()` (codegen.cpp), which walks a chain rooted in a bare scalar variable and confirms *every* level is a hash-key `ArrowDeref` (never an array-index one), and used it to widen both the hash-target and array-target autoviv dispatch conditions alongside the pre-existing `isElemRootedChain()` check. Safe by construction: the pre-existing, generic `emitAutovivContainer()` helper's recursion already bottoms out correctly at a bare scalar variable (a plain, non-autovivifying deref — safe, since a scalar variable itself is never `FLAT_ARRAY`-tagged), and at each hash-key level only ever calls `perl_hash_autoviv_hash`/`perl_hash_autoviv_array` — never `perl_array_autoviv_array`, the one D40 identified as unsafe to call on an array that might hold a `FLAT_ARRAY`-tagged element. This also fixes the "mixed" case `$ref->{x}[0] = val` (hash keys down to a scalar ref, ending in an array-index assignment) for the identical reason — that pattern never touches `perl_array_autoviv_array` either, only `perl_hash_autoviv_array`. Verified: 2-level and 3-level scalar-ref-rooted chains, the mixed hash-then-array-index case, missing-path reads still correctly return `undef` (no autoviv on read), an existing sibling key is preserved (not overwritten) when autovivifying a new one, independent branches under the same ref don't interfere, and the original element-rooted case (D40) is unaffected (regression). `nb.pl` (`FLAT_ARRAY`-heavy) still compiles, runs, and produces correct output with no observable performance change, confirming the new, narrower dispatch condition doesn't affect that code path. **Still open, deliberately not fixed**: a scalar-ref-rooted chain with an array-index level anywhere (e.g. `$ref->[0][1] = val`) still silently fails — extending the fix there would require `perl_array_autoviv_array` itself to become `FLAT_ARRAY`-aware (recognizing and correctly working with a `FLAT_ARRAY`-tagged element instead of blindly overwriting it), a more invasive runtime change out of scope here. Tests: `tests/scalar_ref_autoviv_smoke.pl` (1 check) and `tests/scalar_ref_autoviv.pl` (11 checks across 8 sections) — verified byte-for-byte against real Perl. |
| D51 | `\$` inside a double-quoted string literal doesn't produce a literal `$` correctly — silently drops characters | **FIXED (2026-07-12)** | Found while fixing D23 (regex backslash handling). `my $x = "price: \$100"; print $x;` printed `price: 00` instead of real Perl's `price: $100`. Root cause: the lexer's `readString()` collapsed `\$`/`\@` to a bare `$`/`@` character while scanning, but the *parser's* interpolation scanner (`parseStringInterp`) then re-scans that same raw text for a bare `$`/`@` to trigger interpolation — an escaped-literal `$` was byte-for-byte indistinguishable from a real trigger by the time it got there, so `$100`'s digits got misread as the `$1` capture variable followed by literal `00`. Fixed by having `readString()` emit a `\x02` marker byte (never otherwise producible in that buffer) immediately before an escaped-literal `$`/`@` instead of collapsing to a bare character, and giving `parseStringInterp` a new first-checked case recognizing that marker. A plain backslash was considered and rejected as the marker: `"a\\$x"` (an escaped backslash immediately followed by a genuine, unescaped `$x`) already collapses `\\` to one literal `\` one step earlier in the same switch, which would be indistinguishable from an escaped `\$` under a plain-backslash-marker scheme — confirmed this exact ambiguity empirically before settling on `\x02`. **Two more bugs found and fixed in the same investigation, same function**: single-quoted strings previously ran through the *identical* escape-resolution switch as double-quoted ones, so `'a\nb'` incorrectly became `a`+newline+`b` instead of real Perl's 4 literal characters `a`,`\`,`n`,`b` (single-quoted strings only ever recognize `\\` and `\'` as escapes) — `readString()` now takes an explicit `interpolates` flag from each of its three call sites instead of inferring escape behavior from the delimiter character; and `qq(...)`/`q{...}`-with-a-non-brace-delimiter (`qq(...)`, `qq[...]`, `qq/.../`) hardcoded `'}'` as the closing delimiter regardless of the actual one used, so `qq(hello world)` silently produced no output at all — fixed by computing the correct close delimiter from the actual open one, mirroring `qw()`'s existing open→close mapping a few lines above in the same file. **Separate, unrelated, pre-existing bug found and NOT fixed** — logged as new **D60**: bare `q(...)`/`q[...]` (single-`q`, non-brace, non-`qq` delimiter) isn't recognized as a quote-like operator at all and is a hard parse error; confirmed unrelated to this fix by reproducing identically with these changes `git stash`ed out. Tests: `tests/string_escape_smoke.pl` (1 check) and `tests/string_escape.pl` (14 checks across 8 sections: escaped `$`/`@` before digits/words, multiple escapes in one string, the ambiguous escaped-backslash-then-real-interpolation case, escaped-and-real interpolation combined, plain-interpolation regression, single-quoted escape semantics, `qq()` with paren/bracket delimiters) — verified byte-for-byte against real Perl. |
| D52 | `$@ = "..."` is a silent no-op — `$@` isn't wired up as an assignment target at all | **FIXED (2026-07-15)** | Found while fixing D35 (Carp::croak). After `$@` has been set by a caught `die`/`croak`, explicitly clearing it with `$@ = "";` did nothing — `$@` kept its old value. Root cause: `$@` parses to its own distinct `NK::DollarAt` AST node (not `NK::ScalarVar` with name `"@"`, the way `$/` and `$!` do — confirmed in `parser.cpp`'s `$@` lexing, ~line 1710), so it never matched the special-global assignment case already handling `$/`/`$!`; `emitLValue()` (codegen.cpp) had no `case NK::DollarAt` either, only `ScalarVar`/`ArrayElem` (falling to `default: return nullptr`), and the generic `NK::Assign`/`NK::CompoundAssign` codegen silently no-ops when `emitLValue` returns null. Fixed two ways for the two call shapes: (1) plain `$@ = expr` gets a dedicated early-return case in `NK::Assign` right alongside the existing `$/`/`$!` special-global case, calling `perl_assign(perl_get_dollar_at(), rhs)` directly; (2) `emitLValue()` gained a `case NK::DollarAt` that wraps `perl_get_dollar_at()`'s stable pointer in a fresh alloca "slot" (matching the alloca-based lvalue contract every other caller of `emitLValue` expects), which transparently makes compound-assignment forms (`$@ .= "x"`, `$@ ||= "y"`) work too via the exact same generic path as ordinary scalars, no special-casing needed there. **Second, related bug found and fixed alongside this one**, discovered while testing a realistic nested-eval `$@`-clearing idiom: perlc's `EvalBlock` codegen reset `$@=""` *before* running the eval body, instead of *after* successful completion the way real Perl does — real Perl's post-success reset unconditionally overrides anything the block itself did to `$@` (confirmed directly: `eval { $@="outer" }; eval { $@ = "manually cleared"; }; print $@;` prints empty string in real Perl, since the *inner* eval's own success clears it right after, regardless of the in-block assignment). With the pre-body clear, once `$@` became a real assignment target this fix's own first version wrongly let an in-block assignment "stick" instead of being clobbered by eval's own success-clear, diverging from real Perl. Fixed by moving the `$@=""` reset from immediately after `perl_eval_push` (unconditional, pre-body) to immediately before each success-path `br` to the shared `eval.end` block (both places `bodyResult` gets stored — the normal case and the "nested eval as non-last statement" case) — the longjmp/die path still jumps straight to `eval.end` without passing through either of those points, so a caught `die`'s `$@` message (set by `perl_die` itself) is correctly left untouched. Verified against real Perl: basic clear after a caught exception, assigning an arbitrary string, `.=` compound assignment, assigning a numeric value, truthiness before/after clearing, a clean (non-dying) eval's existing reset (regression), the in-block-assignment-overridden-by-success-clear case above, `$@` surviving a sub call that reads it before the caller reassigns it, persistence across subsequent unrelated statements, and a regression check that `die` inside `eval` still correctly populates `$@`. **New, separate, previously-undocumented defect found while writing this fix's regression test, logged as D89, NOT fixed here**: `die` with a message lacking a trailing newline never appends Perl's standard `" at FILE line N.\n"` suffix, true of both the eval-caught and uncaught/top-level paths. Tests: `tests/d52_error_var_assign_smoke.pl` (1 check) and `tests/d52_error_var_assign.pl` (10+ checks across 10 sections) — verified byte-for-byte against real Perl. Full `tests/harness.sh` regression suite re-run after the fix: 131/138 passing, same 7 pre-existing failures, zero regressions. |
| D53 | `my $r = \$x; $r = $r;` (self-assignment of a reference to a block-scoped lexical) gives the wrong dereferenced value | **FIXED (2026-07-15)** | Originally found while fixing D35's self-assignment crash and described as a self-assignment bug (`$r = $r;` specifically breaking the reference), only reproducing inside a nested `{ }` block. **Re-investigation while fixing this found the self-assignment step was a red herring**: the identical failure reproduces with no self-assignment involved at all — `my $x = 42; my $r = \$x; $$r = 99; print $x;` alone (no `$r = $r;` anywhere) already printed the stale `42` instead of `99`. Confirmed via `--emit-ir` that the actual root cause is architectural: a block-scoped `my $x = <literal>` gets placed on perlc's unboxed int/float fast path (`intScopes_`/`floatScopes_`), an optimization that skips allocating a real, addressable `PerlValue*` for the variable at all, keeping just a plain `i64`/`double` LLVM value instead — every subsequent *read* of `$x` in the compiled IR was a hardcoded `perl_alloc_int(42)` constant, completely disconnected from whatever `$$r=99` had actually written. `\$x`'s codegen reads `$x` through the ordinary scalar-read path, which "boxes on demand" for a fast-pathed variable: it allocates a *fresh, disposable* `PerlValue*` containing a *copy* of the current fast-path value and returns that — `perl_ref_scalar()` then creates a reference to that one-off copy, not to any real storage for `$x` (which doesn't exist), so writing through the reference correctly mutates the disposable copy while `$x`'s actual fast-path storage — and every later plain read of `$x` — remains completely untouched. The identical pattern at file scope doesn't reproduce because file-scope `my`/`our` variables always get a real `GlobalVariable`, never using this fast path at all — confirming the fast path was the actual site of the bug, not scope depth or self-assignment. **This is the exact same underlying architectural gap D64 already found and fixed for closures** (`sub {...$x...}` capturing a disconnected snapshot of an unboxed variable) — D53 is the identical gap, just independently discovered via `\$x` instead of closure capture. Fixed by extending the existing `collectClosureCapturedNames` pre-scan (added for D64) to also collect the target name of any `\$name` (`NK::RefScalar`) found anywhere in the current function, not just names used inside a nested closure — both cases now share the exact same `capturedNamesInCurrentFn_` set and the exact same gating check at the `my $var = <literal>` declaration site (`codegen.cpp`'s `case NK::My:`), so a variable that's ever referenced via `\$var` anywhere in its function is declared as a normal, fully-addressable boxed scalar from the start, never placed on the unboxed fast path at all. Verified extensively, byte-for-byte against real Perl: the original repro (with the self-assignment step, still correct); the same repro with the self-assignment step removed (the actual root cause, isolated); a float variable (the fast path's separate `floatScopes_` branch); a bare read through a reference with no write (regression — already worked before this fix); multiple references to the same variable all seeing a write made through any one of them; the variable still usable in ordinary arithmetic after a reference to it has been taken; a reference to a sub's own lexical, written through, correctly affecting the sub's return value; the plain unboxed-fast-path loop case with no reference taken at all (regression — the fast path's actual performance-motivating use case, confirmed still used and still correct); closures capturing an unboxed variable (regression — D64's own fix, confirmed unaffected); and the file-scope case (regression — already correct, confirmed still correct, never used this fast path either way). Tests: `tests/d53_ref_alias_smoke.pl` (1 check) and `tests/d53_ref_alias.pl` (12+ checks across 10 sections) — verified byte-for-byte against real Perl. Full `tests/harness.sh` regression suite re-run after the fix with zero new failures. |
| D54 | `make test-tsan`'s TSan-instrumented *compiler binary* (`perlc_tsan`) hangs compiling `tests/threads.pl`, specifically after the D25 fix | **OPEN** | Not a runtime thread-safety issue — `wchan=futex_do_wait` (blocked on a futex, not busy-looping); `TSAN_OPTIONS=die_after_fork=0` gets past the hang (output binary produced successfully), pointing at a TSan+`fork()` interaction in `perlc`'s own subprocess invocation of `clang-18` for linking. Bisected directly: the immediately prior commit (before D25) compiles the same file in ~3.6s with no hang. But `tests/threads.pl` uses no `eval` at all, so D25's actual logic changes (`NK::Return`/`NK::EvalBlock`/`NK::AnonSub`) are never exercised by that file's compiled output — most likely a subtle change in the compiler binary's memory layout/timing (from the added code) newly exposing a latent, probabilistic TSan+fork race in the *compiler's own* process, not a logic bug. Correctness of the D25 fix itself is separately and thoroughly verified via `tests/harness.sh` with the regular (non-TSan) `perlc`, including `threads.pl`/`threads_atomic.pl` passing their own internal self-checks. **New manifestation observed during D64 (2026-07-14)**: with D64's changes present, `perlc_tsan` compiling `tests/threads.pl` no longer hangs but instead reliably (2/2 runs) SEGVs during the compiler's own `CodeGen::~CodeGen()` destructor cleanup (freeing an `unordered_map<string, llvm::Function*>`), reported by TSan as "signal-unsafe call inside of a signal" — the identical underlying TSan+fork/signal interaction in the compiler's own process, just a different symptom (crash vs. hang) of the same latent race, now apparently made deterministic-in-this-environment by D64's changes shifting the compiler binary's memory layout/timing (consistent with this entry's own prior bisection finding that unrelated code changes can newly expose or suppress this race). Confirmed via `git stash`: reproduces 0/2 times without D64's changes, 2/2 times with them present — but the crash site (LLVM `Function*` hashtable teardown at process exit) has zero connection to D64's actual changes (`collectClosureCapturedNames`, a pure AST-walk building a name set — no signals, no forking, nothing thread-related), reinforcing that this is a timing-sensitive manifestation of the same pre-existing compiler-process issue rather than a new logic bug. `TSAN_OPTIONS=die_after_fork=0` again gets past it; the resulting binaries for all three TSan test files (`threads.pl`, `threads_atomic.pl`, `destroy.pl`) run and pass every internal self-check with zero TSan race warnings, confirming D64's changes introduce no actual thread-safety regression in generated programs — only in this already-documented compiler-tooling artifact. |
| D58 | `our $pkgvar` (or a package-qualified sub redefinition) does not persist/update across *repeated* `do FILE` calls on the same file within one process | **PARTIALLY FIXED (2026-07-12)** | Found while verifying D24's `do FILE` implementation. **Scalar case: FIXED.** File-scope `our`/`my` scalars in a `--do-lib` compile now resolve through a new process-wide registry (`perl_get_or_create_global_scalar()`, runtime.c — same design as the pre-existing `perl_register_method`/`perl_call_named_sub` table used for cross-do-lib sub calls) instead of a plain per-compilation-unit `GlobalVariable`, keyed by the qualified `Package::name`. Gated strictly behind a new `asDoLib_` CodeGen flag — normal (non-`do`-related) compilation is completely untouched, zero risk to the vastly more common case. Verified: a bare `our $counter; $counter++;` now correctly accumulates (1, 2, 3, ...) across repeated `do` calls on the same file, matching real Perl exactly; an explicit initializer (`our $x = 5;`) still correctly *re-runs* on every `do` call rather than only-once (matching real Perl: the initializer is an ordinary assignment, not run-once magic); cross-package access (`$Foo::x` from `package main`) within do-lib-compiled code still resolves correctly. **Sub-redefinition case: confirmed still open, not fixed** — `perl_call_named_sub()` still returns the *first* registered match, so redefining a sub via a second `do` of a modified file still doesn't take effect (verified directly: real Perl's second call picks up the new definition, perlc's doesn't) — fixing this would need `perl_register_method`/`perl_call_named_sub` to support overwrite-on-reregistration, a distinct, still-narrower follow-up left for its own pass given it wasn't part of the original scalar-persistence report and array/hash file-scope globals (`fileArrayGlobals_`/`fileHashGlobals_`) have the identical un-fixed root cause too. **A further, deeper, out-of-scope limitation found while testing** (not a regression from this fix — was equally true before it, and is a natural consequence of perlc's compile-time package-variable model rather than a discrete bug): the separately-compiled *loading* program cannot access a package variable that only a dynamically do'd file ever declares (`$Foo::x` from the caller's own statically-compiled code, where `Foo::x` was never mentioned anywhere in the caller's own source) — perlc's `$Package::var` resolution is populated at compile time from `our`/`my` declarations actually present in that same compilation unit, so a variable the caller has zero static knowledge of can't resolve; real Perl's dynamic symbol-table (`%stash`) model has no such restriction. Fully closing this would need a much broader change (every `$Package::var` reference falling back to a runtime registry lookup when no static declaration is known, not just `our`/`my` declarations *within* do-lib code) — out of scope here. Tests: `tests/do_file_persistence_smoke.pl` (1 check) and `tests/do_file_persistence.pl` (7 checks across 5 sections: bare-`our` accumulation across 3 repeated `do` calls, explicit-initializer re-run-every-time, cross-package access verified from *within* the do'd file itself, single-`do` regression, normal-non-do-lib-program regression) — verified byte-for-byte against real Perl. |
| D59 | `$$` (PID special variable) never interpolates correctly inside a double-quoted string — always produces literal `$$`/mangled output instead of the PID | **FIXED (2026-07-13)** | Found while writing regression tests for D58 (`"file_$$.pl"` produced `"file_$0pl"` instead of e.g. `"file_12345.pl"`). Confirmed the bug was specific to *string interpolation*: `$$` as a bare expression (`my $p = $$;`) always worked correctly and returned the real PID; only `"...$$..."` inside a double-quoted string was broken, in every position tested (`"pid=$$"` → `"pid=$$"` unchanged, `"pid=$$ end"` → unchanged, `"pid=$$here"` → `"pid=$"`, `"file_$$.pl"` → `"file_$0pl"`). Root cause: `parseStringInterp`'s raw-text scanner (parser.cpp) had *no case at all* for `$` immediately followed by a second `$` — none of its existing special-variable checks (`$@`, `$.`, `$,`, `$!`, `$/`, `$&`, `$0`, `$1`-`$9`, `${...}`, `$name`) match that pattern, so it fell through to the generic fallback, which appends only the *first* `$` as a literal character and rescans from the second `$` on the next loop iteration — that second `$` then matched whatever *other* rule its own following character happened to satisfy (e.g. `.` right after matched the unrelated `$.`, input-line-number, special variable, garbling `"file_$$.pl"` into `"file_$0pl"`). Fixed with a dedicated `$$` case mirroring the token-level parser's (`parsePrimary`) already-correct handling for a *bare* `$$` expression: `$$` followed by a letter/underscore is a scalar dereference (`${$word}`, `NK::DerefScalar`); `$$` followed by anything else, including end-of-string, is the PID (`NK::GetpidFunc`). Verified directly against real Perl (self-checking against the same process's own bare `$$` value, since a PID can't be hardcoded): `$$` alone, before a space, before punctuation (the original repro), at the very end of the string, `$$word` deref of a defined ref, combined with an ordinary `$var` interpolation in the same string, multiple `$$` occurrences in one string, and a plain-interpolation regression check. Also re-verified every pre-existing test file that already used `"...$$..."` for temp-filename uniqueness (`fileops.pl`, `low_risk_fixes.pl`, `low_risk_fixes_smoke.pl`, `tier2.pl`, `tier3.pl`, `dbi_sqlite.pl`) — all still pass byte-for-byte (they never printed the garbled/fixed string itself, only used it internally as *some* unique-ish path, so this fix is purely additive there). Distinct root cause from D51 (`\$` *escaping*, not `$$` interpolation) and D53 (`$$r` *dereference* syntax, not the PID special variable) despite the superficial `$$` similarity to both. **New, separate, pre-existing defect found while scoping this fix and NOT fixed here** — logged as new **D63**: a *subscripted* dereference immediately after `$$` (`$$ref[0]`/`$$ref{k}`, real Perl's shorthand for `$ref->[0]`/`$ref->{k}`) produces the wrong (empty/undef) result — and this is not new or string-interpolation-specific: the equivalent *bare* expression (`my $x = $$aref[1];`, no string involved at all) already silently produces the wrong result under perlc today. Traced far enough to know it's a real, distinct root cause from D59: `$$aref[1]`'s compiled IR calls `perl_deref_scalar($aref)` (treating `$aref` as if it were itself a `REF_SCALAR`, wrong — it's a `REF_ARRAY`) and *then* `perl_deref_array()` on that (wrong) result — a double-deref, instead of the single `perl_deref_array($aref)` real Perl's `$aref->[1]`-equivalent semantics require. This fix's own test suite (both the smoke and in-depth files) deliberately only exercises the un-subscripted `$$word` deref form for exactly this reason. Tests: `tests/dollar_dollar_interp_smoke.pl` (1 check) and `tests/dollar_dollar_interp.pl` (8 checks across 8 sections) — verified byte-for-byte against real Perl. Full regression suite: 95/102 pass (up from 93/100 — 2 new test files added and both passing, the same 7 pre-existing failures unchanged, no new regressions). |
| D63 | `$$ref[idx]`/`$$ref{key}` (dereference-then-subscript shorthand for `$ref->[idx]`/`$ref->{key}`) produces the wrong (empty/undef) result — not string-interpolation-specific, the bare expression form is broken too | **FIXED (2026-07-13)** | Found while scoping D59's fix (string interpolation of `$$`). `my $aref = [10,20,30]; my $x = $$aref[1];` silently gave `undef` under perlc instead of real Perl's `20` — confirmed this reproduced identically as a completely bare expression with no string interpolation involved at all, so it was unrelated to D59's own root cause (the interpolation scanner) despite the superficial `$$...[...]` similarity. Root cause: the parser already correctly built a `DerefScalar` AST node for a bare `$$ref` (real Perl: `${$ref}`), but `parseSubscript`'s *adjacent-subscript* handling (parser.cpp — the grammar production for `EXPR[idx]`/`EXPR{key}` immediately following any of a list of "subscriptable" expression kinds, `DerefScalar` among them) took that `DerefScalar` node and used it *directly* as the base of a further `ArrowDeref`, compiling as "dereference the base value (already itself the result of dereferencing $ref!) as an array/hash, then index it" — an incorrect second dereference. Confirmed via `--emit-ir`: the compiled code called `perl_deref_scalar($aref)` (treating `$aref` as if it held a `REF_SCALAR`, wrong — it's a `REF_ARRAY`) and *then* `perl_deref_array()` on that wrong result. Real Perl's actual rule for `$$ref[idx]` is *one* level of indirection total: `$ref` itself (not `${$ref}`) is the array/hash reference to index — exactly `$ref->[idx]`. Fixed by special-casing exactly this situation in `parseSubscript`: when the base about to receive an *adjacent* `[idx]`/`{key}` subscript is a bare `DerefScalar`, unwrap it back to its own inner scalar-variable node (`$ref` itself) before building the `ArrowDeref`, instead of using the `DerefScalar` wrapper as-is; this only fires for the specific adjacent-bracket grammar production with no intervening `->` — the genuinely different, already-correctly-working double dereference `$$ref->[idx]` (real Perl: dereference $ref as a scalar to get a *second* reference, then arrow-deref *that*) goes through a separate branch of the same function, untouched by this fix. A subsequent *chained* subscript (`$$aref[0][1]`) is also unaffected, since by then the base is already an `ArrowDeref`, not a `DerefScalar` — the unwrap only fires once, for the base case. Verified directly against real Perl: array/hash element reads, both as *lvalues* too (`$$aref[i] = val`, `$$href{k} = val` — the same AST fix correctly threads through `emitLValue` as well as `emitExpr`, since both consume the identical, now-corrected AST shape), 2-level nested chains for both array-of-arrays and hash-of-hashes, and — critically, as regressions — the explicit-arrow double-deref form, a plain un-subscripted `$$ref`, and ordinary `$name[idx]`/`$name{key}` (no `$$` at all) all continue to work exactly as before. **NOT fixed here** (a separate, deeper, pre-existing gap, already flagged as out of scope by D59's own notes): a subscripted dereference *inside string interpolation* (`"$$ref[0]"`) still doesn't work — `parseStringInterp`'s raw-text scanner is an entirely separate code path from the token-level parser this fix touches, and doesn't attempt to recognize a subscript following `$$word` at all; this fix's own test suite deliberately builds its lvalue-write-then-read-back check via string concatenation rather than interpolation for exactly this reason. Tests: `tests/deref_subscript_smoke.pl` (1 check) and `tests/deref_subscript.pl` (10 checks across 9 sections: array/hash reads, array/hash lvalue writes, nested array-of-arrays and hash-of-hashes chains, and 4 regression checks) — verified byte-for-byte against real Perl. Full regression suite: 97/104 pass (up from 95/102 — 2 new test files added and both passing, the same 7 pre-existing failures unchanged, no new regressions). |
| D60 | Bare `q(...)`/`q[...]`/`q<...>` (single-`q`, non-`{`, non-`qq` delimiter) is not recognized as a quote-like operator at all — hard parse error | **FIXED (2026-07-14)** | Found while testing D51's fix. `my $x = q(literal $var here);` gave `Error: Parse error line N: expected ) but got '$'` — the lexer's `q{}`/`qq{}` recognition (lexer.cpp) only triggered for `c=='q' && (peek(1)=='{' \|\| peek(1)=='q')`, i.e. a bare `q` was only recognized when *immediately* followed by `{` (brace) or another `q` (the `qq` prefix) — a bare single-`q` followed directly by any *other* delimiter character (`(`, `[`, `<`, `/`, etc.) fell through to ordinary bareword lexing, choking on the first non-expression token inside. Confirmed pre-existing and unrelated to D51's changes. Fixed by widening the recognition condition to also match `q`/`qq` followed by a small, deliberately conservative set of bracket/slash delimiters — `( [ < /` — rather than "any non-alphanumeric punctuation": `q` is a single short letter that legitimately appears immediately before many *other* punctuation characters in unrelated contexts, most importantly a hash-subscript bareword key (`$h{q}`, where the character after `q` is `}`) — confirmed empirically that this already works today and must keep working, so `}` (and `)`, `]`, `>` as *openers*, which would be bizarre/unheard-of in practice) were deliberately excluded from the widened set. **A second, more severe, previously-undiscovered bug was found and fixed in the same investigation, same code block**: plain `q{...}` (not `qq{...}`) was *itself* already silently broken, independent of any delimiter-recognition gap — `q{hello world}` produced no output at all under perlc. Root cause: the shared `pos_ += 2;` after recognizing the `q{`/`qq{`/`qq<delim>` pair unconditionally skipped two characters — correct for genuine `qq` (both letters of the prefix), but wrong for plain `q` immediately followed by a real delimiter, where skipping 2 consumed the delimiter itself along with the `q`, leaving the balanced-brace-scan's own `if (peek() == '{')` check looking at the first *content* character instead of the actual delimiter and silently falling through to the wrong branch. Fixed by skipping only 1 character for the non-`qq` case, leaving the delimiter in place for that check and the existing `qopen`/`qclose` fallback to see correctly, exactly as they already did for `qq`. Verified directly against real Perl: `q()`/`q[]`/`q<>`/`q//` with content, empty `q()`, the newly-fixed plain `q{...}` case, `qq(...)` (D51) regression, `qw(...)` regression, the hash-key-bareword-`q` regression (confirming the narrow delimiter-set choice), and that bare `q(...)` correctly does *not* interpolate (unlike `qq(...)`). **New, separate, pre-existing defect found while scoping this fix and NOT fixed here** — logged as new **D65**: a double-quoted string interpolating a scalar variable whose name *starts* with "qq" (e.g. `$qqfoo`), followed later in the same string by a literal `]`/`)`/`}` character, produces a spurious parse error — confirmed via a temporary git-worktree bisection against a commit predating this entire session's work to be a long-standing, completely unrelated defect (the failure is inside ordinary variable interpolation, nowhere near quote-like-operator delimiter recognition), not investigated further. Tests: `tests/bare_q_delimiter_smoke.pl` (1 check) and `tests/bare_q_delimiter.pl` (10 checks across 10 sections: the four newly-supported delimiter forms, empty content, the plain-`q{}` regression, `qq(...)`/`qw(...)` regressions, the hash-key-bareword-`q` regression, and the no-interpolation check) — verified byte-for-byte against real Perl. Full regression suite: 103/110 pass (up from 101/108 — 2 new test files added and both passing, the same 7 pre-existing failures unchanged, no new regressions). |
| D65 | A double-quoted string interpolating a scalar variable whose name starts with "qq" (e.g. `$qqfoo`), followed later in the same string by a literal `]`/`)`/`}` character, produces a spurious parse error | **FIXED (2026-07-14)** | Found while scoping D60's fix (bare `q(...)` delimiter recognition), initially thought completely unrelated to any quote-operator syntax — `my $qqfoo = "a"; print "$qqfoo]\n";` failed to compile — but a deeper investigation found the true root cause is squarely in the same lexer neighborhood after all. A bare `$`/`@`/`%` sigil is tokenized as its own, separate single-character token (lexer.cpp) without consuming the identifier that follows — the *next* tokenizer-loop iteration reads that identifier via the generic bareword path, which runs *after* the `q`/`qq`/`qw` quote-like-operator recognition checks earlier in the same loop. Those checks had no way to distinguish "a fresh expression-start `q`/`qq`/`qw`" from "the first letter(s) of an ordinary identifier that happens to start with q/qq/qw and was simply sigil-prefixed" — so `$qqfoo` (an ordinary variable reference) was caught by the `qq` check meant for `qq(...)`-style quoting. Made worse by a second, compounding gap: unlike the pre-existing `qw` check (which already required the character after "qw" to be non-alphanumeric/non-underscore), the `qq` check had *no* such guard at all — it treated whatever character followed "qq" as a delimiter unconditionally, so `$qqfoo` misread "f" as a delimiter and scanned far ahead in the source looking for another literal "f" to close it, corrupting the token stream; the *specific* symptom (a hard parse error vs. a silently-wrong-but-still-parseable result) depended entirely on incidental nearby text happening to contain (or not) another instance of that same misidentified "delimiter" character — which is also why an initial bisection of the trigger character found a seemingly inconsistent pattern (some third-letters "worked", most didn't) that turned out to be pure coincidence of surrounding text, not a genuine exception, confirmed by constructing an adversarial case where even a previously-"safe" third letter (`a`) failed once the surrounding text changed. Also found, while characterizing the true scope: the `%` (hash) sigil has the exact same "bare sigil, identifier read separately" structure as `$`/`@`, so even a hash *declaration* (`my %qw = (...)`) had its own "qw" identifier misread as a `qw()`-list operator, and a variable literally named `q` or `qw` used for hash-element access outside of string interpolation (`$q{key}`, `$qw{key}`, bare, not inside a string) silently evaluated to the wrong value entirely (rather than erroring), since `$q{...}` hit the pre-existing (already-narrow, but sigil-unaware) `q{...}` brace-delimiter path. Confirmed the underlying defect predates this entire session's work via a temporary `git worktree` checkout of a commit (`b4197f9`) from before any of it. Fixed by computing, once per tokenizer-loop iteration, whether the immediately preceding token was a bare `$`/`@`/`%` sigil (single-character token text — the only shape a freshly-pushed, not-yet-extended sigil token ever has) and unconditionally skipping all `q`/`qq`/`qw` recognition when so; also added a `qw`-style non-alphanumeric/non-underscore guard to the `qq` branch itself, for robustness at expression-start positions too (matching the guard `qw` already had). Verified directly against real Perl: the original bracket/paren/brace-after-`$qqfoo` cases, `$q{key}`/`$qw{key}` bare hash-element access, a hash *declared* with the name `qw`, and regressions for genuine `qq(...)`, `qw(...)`, and D60's bare `q(...)` at expression-start position, plus ordinary q-prefixed variable names elsewhere. **New, separate, pre-existing, and potentially higher-impact defect found while writing this fix's own tests, logged as new D66, NOT fixed here**: a block-scoped `my $x = $hash{key};` where the hash's value is a *string* silently coerces it to `0` — confirmed unrelated to variable naming (reproduces for any hash name) and root-caused to `canEmitF64`'s `NK::HashElem` case (codegen.cpp) unconditionally returning `lookupHash(n.name) != nullptr` — i.e. "this can be treated as a pure double as long as the hash variable exists," with no check at all on whether the actual value at that key is numeric. Tests: `tests/qq_sigil_collision_smoke.pl` (1 check) and `tests/qq_sigil_collision.pl` (10 checks across 8 sections: the original bracket-after-`$qqfoo` case plus paren/brace variants, `$q{key}`/`$qw{key}` bare hash access, a hash declared named `qw`, `qq(...)`/`qw(...)`/bare-`q(...)` regressions, and other q-prefixed variable names) — verified byte-for-byte against real Perl. Full regression suite: 105/112 pass (up from 103/110 — 2 new test files added and both passing, the same 7 pre-existing failures unchanged, no new regressions). |
| D66 | A block-scoped `my $x = $hash{key};` where the hash's value is a *string* silently coerces it to `0` instead of the actual string | **FIXED (2026-07-14)** | Found while writing D65's fix tests; confirmed unrelated to variable/hash naming (reproduces identically for any hash name, e.g. `%other`, not just `q`/`qw`-prefixed ones) and unrelated to D65's own lexer-level fix. `{ my %h = (key => "strval"); my $x = $h{key}; print "$x\n"; }` printed `0` under perlc instead of real Perl's `strval`. Confirmed the trigger required *both* block scope (the identical code at true file scope worked correctly) *and* a string-typed hash value (the identical block-scoped code with a numeric hash value, e.g. `(key => 42)`, also worked correctly). Root-caused to `CodeGen::canEmitF64`'s `case NK::HashElem` (codegen.cpp ~1929): `return lookupHash(n.name) != nullptr;` — i.e. a hash-element access was considered safe to compile via the unboxed-double fast path (`case NK::My:`'s numeric-literal-detection branch, the same fast path implicated in D64) as long as the *hash variable itself* was in scope, with no check whatsoever on whether the *value actually stored at that key* was numeric; the fast path then called `perl_to_float()` on whatever `perl_hash_get_str_ref` returned, which silently gave `0.0` for a non-numeric string. File-scope declarations were unaffected only because `case NK::My:`'s fast-path attempt is gated behind `!atFileScope` already (established during D64's investigation) — file-scope assignments never reached `canEmitF64` for this at all, not because the underlying `HashElem` check was actually safe. **Fixed** by making `canEmitF64`'s `NK::HashElem` case unconditionally return `false` and `emitExprF64`'s matching case unconditionally return `nullptr` (falling back to the general, tag-checking `emitExpr` path), mirroring the conservative treatment `NK::ArrayElem` and the hash-`ArrowDeref` case already receive a few lines away in the same file — no hash-specific value-type tracking analogous to `arrayElemTypes_` was built; the fast path is simply disabled for hash elements rather than selectively re-enabled, trading away a narrow optimization for correctness. Verified numeric hash values (`int`/`float`, including subsequent arithmetic on the result) remain correct after the fix. **New, separate, pre-existing defect found while writing the regression test, logged as D83, NOT fixed here**: `ref()` returns empty string instead of `"ARRAY"` for a scalar holding a FLAT_ARRAY-tagged value — confirmed unrelated to this fix (reproduces identically with or without it, and with no hash involved at all). Tests: `tests/d66_hash_elem_string_smoke.pl` (2 checks) and `tests/d66_hash_elem_string.pl` (20+ checks across 15 sections: string/int/float values, arithmetic, interpolation, differently-named hashes, nested blocks, sub bodies, if/while/for blocks, mixed-type hashes, `undef`, array/hash-ref values, numeric-looking strings, file-scope regression, reassignment) — verified byte-for-byte against real Perl. Full `tests/harness.sh` regression suite re-run after the fix with zero new failures. |
| D61 | `sort { BLOCK }` comparator has *zero* closure-capture support for any outer block-scoped (non-file-scope) lexical variable — silently can't see/modify them at all | **FIXED (2026-07-13)** | Found while testing D28's fix. `{ my @observed; my @data=(3,1,2); my @s = sort { push @observed, $a; $b<=>$b } @data; }` — `@observed` stayed empty (`push` inside the comparator silently had no effect on the outer array) when `@observed` was declared inside a bare `{ }` block; the identical code worked correctly when `@observed` was declared at true file scope instead. Confirmed pre-existing (reproduced identically with D28's fix `git stash`ed out) and specific to `sort`'s comparator: `map`/`grep`/`reduce` blocks (all compiled *inline*, sharing the enclosing scope stack) have no such issue — only `sort`'s comparator is compiled as a genuinely separate LLVM function (`cmpFn`, needed since it's passed as a real C function pointer to the underlying `qsort`-based sort routine), and that function's scope maps are fully reset (`scopes_ = {}` etc., codegen.cpp) with no closure-capture mechanism wired up to reach back into the enclosing scope the way `AnonSub` already has. Fixed by giving the comparator real closure-capture support, mirroring `AnonSub`'s own Phase-1/Phase-3 pattern: before the comparator's scope reset, `collectAllScalarNames()` finds every scalar name its body uses (excluding `$a`/`$b`, bound separately by D28's own logic) and every array/hash currently visible in scope is captured too (over-capture, same rationale `AnonSub` already documents); each capture is pushed into a `PerlArray*` via the existing `perl_array_push_capture`, and `perl_sort_custom()` (runtime.c) was extended to take that array and install it into the same `s_current_captures`/`s_ncaptures` thread-local context `perl_call_code_ref()` already uses for ordinary closures — save/restore around the `qsort()` call so a nested `sort{}`/closure call from inside the comparator can't corrupt it — so the comparator body can call `perl_get_capture(idx)` exactly as an `AnonSub` body already does. `sort SUBNAME LIST`'s separate comparator path is deliberately untouched (it correctly uses only file-scope package globals for `$a`/`$b`, matching real Perl's actual semantics for a named, separately-scoped sub) other than a mechanical update for `perl_sort_custom`'s new third parameter (passed an empty captures array). Verified extensively: block-scoped array push (the original repro, now non-empty and every pushed value confirmed a real member of the input list — algorithm-independent, since qsort()'s exact invocation count/pairing still legitimately differs from real Perl per D28's own notes), hash-key capture, two independent captured arrays in one comparator, nested (2-level) block depth, and that a comparator capturing nothing (the common case) is unaffected — all with byte-for-byte-correct final sort order for every non-degenerate (valid strict-weak-ordering) comparator tested. **New, separate, pre-existing defect found while writing this fix's tests and NOT fixed here** — logged as new **D62**: `perl_array_push_capture()` (runtime.c) clones the captured scalar's value instead of storing the original pointer (despite its own comment claiming "(no clone)"), so *any* closure in this codebase — `AnonSub` included, confirmed with a plain, sort-unrelated `sub {}` and confirmed pre-existing via `git stash` — can capture a scalar for reading but can never have a reassignment (`$x = ...`) or increment (`$x++`) made from inside the closure propagate back to the caller (array/hash captures are unaffected, since those wrap a ref-counted pointer to the *same* underlying struct rather than cloning a value). This fix's own test suite therefore deliberately exercises only array/hash mutation from inside the comparator (matching D61's own literal repro), not scalar reassignment. Tests: `tests/sort_comparator_closure_smoke.pl` (2 checks) and `tests/sort_comparator_closure.pl` (13 checks across 7 sections: block-scoped array push with algorithm-independent value validation, hash-key capture, two independent array captures in one comparator, no-capture regression, file-scope regression, nested-block-depth capture, and over-capture-of-the-input-list safety) — verified byte-for-byte against real Perl. Full regression suite: 93/100 pass (up from 91/98 — 2 new test files added and both passing, the same 7 pre-existing failures unchanged, no new regressions). |
| D62 | `perl_array_push_capture()` clones a captured scalar's value instead of storing the original pointer, despite its own comment saying "(no clone)" — closures can read a captured scalar but never see a reassignment/increment made to it from either side propagate to the other | **FIXED (2026-07-13)** | Found while testing D61's fix, but entirely unrelated to `sort`: reproduced identically with a plain, ordinary `AnonSub` closure (`my $x = 0; my $f = sub { $x++ }; $f->(); $f->(); print $x;` printed `0` under perlc instead of real Perl's `2`), confirmed pre-existing by reproducing it with D61's changes `git stash`ed out. An initial investigation (2026-07-13) git-blamed `perl_array_push_capture` to its sole introducing commit (`487575a`, Stage 38: `threads::shared`) and confirmed via a destructive local experiment (clone removed, rebuilt, tested, reverted) that the clone was load-bearing, not dead code: a closure that captures a `my` variable declared in a block which exits *normally* (no `return`), called again after enough intervening allocation traffic to force the freed slab slot back into reuse, genuinely crashed (`free(): invalid size`) once the clone was naively removed — because `CodeGen::popScope()` (codegen.cpp) unconditionally `perl_free()`s a scope's tracked PVs, and a closure holding a raw, unshared pointer into that scope is left dangling once it's freed. Fixed properly with reference counting (full design plan reviewed and approved before implementation): a capture-refcount plus a "scope released" flag packed into the 31 spare bits of `PerlValue.flags` (no struct growth — the 32-byte layout and the `threads::shared` `cmpxchg16b`/`ldxp+stxp` CAS scheme's `offsetof(PerlValue,ival)==8` constraint are untouched). `perl_array_push_capture()` now stores the original pointer and bumps the count instead of cloning; `perl_free()` gained a gate immediately after its existing `PV_FLAG_SHARED` early return — if a PV's capture count is nonzero, it sets `PV_FLAG_CAPTURE_RELEASED` and defers instead of tearing down the payload/pooling it. `PerlClosure` gained its own `refcount` field (mirroring the existing `PerlArray`/`PerlHash` pattern exactly: init to 1 in `make_code_ref_impl`/`clone_code_ref_for_thread`, incremented in `perl_clone`/`perl_assign` wherever a `PERL_CODE_REF`'s `pval` is shallow-copied — previously `perl_free()` had *no* `PERL_CODE_REF` case at all, a documented, deliberate no-op/leak now replaced with real teardown via a new `perl_closure_release()` helper that walks `cl->captures` and releases each one via `perl_release_capture()`, freeing the closure struct itself only once its own refcount reaches 0). `sort { BLOCK }`'s comparator (D61) also captures via the same function but wraps nothing in a `PerlClosure` — there's no teardown event for its captures at all — so `perl_sort_custom()` gained a symmetric release step right before returning; without it, every scalar any `sort{}` comparator captures would be incremented once and never decremented, a new permanent-leak regression. **A second, independent bug was found and fixed during implementation review, before any of this shipped**: `perl_assign()` was discovered to unconditionally reset `dst->flags` to just the pre-existing `PV_FLAG_SHARED` bit on every single assignment (`dst->flags = (unsigned int)shared;`), which would have silently wiped a captured variable's capture-count/released bits the very first time `$x = ...` was ever called on it — since `perl_assign` is exactly how the "stable pointer" model's in-place mutation works, this would have reintroduced the identical dangling-pointer crash on ordinary reassignment. Fixed by preserving the full set of identity-persistent bits (`PV_FLAG_SHARED | PV_CAPTURE_MASK | PV_FLAG_CAPTURE_RELEASED`) across the reassignment instead of just the shared bit. Verified exhaustively: the original same-scope repro (now correctly `2`); two-way visibility (outside mutation seen inside a closure); the exact adversarial escaping-closure crash repro from the original investigation (block exits normally, heavy intervening PV-allocation churn, closure called again afterward) — no crash, correct mutated value, matching real Perl byte-for-byte; multiple independent closures sharing one captured variable, including freeing one early (`undef $g`) while another stays correct; `sort{}` comparator scalar mutation (D61's own tests deliberately avoided this); and 100 repeated `sort{}` calls reusing the same captured scalar with no leak/premature-free. `make test-tsan` (`threads.pl`/`threads_atomic.pl`/`destroy.pl`) stayed completely race-free under `-fsanitize=thread` — the fix explicitly does not touch `clone_code_ref_for_thread`/`thread_wrapper` (thread-isolated captures are freshly `malloc`'d PerlValues that never go through `perl_array_push_capture` in the first place, so they're unaffected either way) and `PV_FLAG_SHARED` cells are excluded from every new code path exactly as they're already excluded from `perl_free`'s freeing. An ASan+UBSan pass (built by hand via direct `clang-18` invocation on the compiled `.ll` + `runtime.c`, since `main.cpp` already recompiles `runtime.c` from source at every link — `valgrind` itself is confirmed impractical, pre-existing SIGILL on this binary's `-mcx16` instructions, unrelated to this fix) found **zero** use-after-free/heap-buffer-overflow/invalid-free/double-free across the new tests plus `sort_comparator_closure.pl` and `closures.pl`; the only LeakSanitizer reports present traced entirely to `PerlArray` pool residency (`pa_alloc`/`perl_array_new`) — confirmed pre-existing and unrelated by reproducing identically on unmodified baseline code with a plain `my @arr = (...)` in a loop and zero closures involved (the deliberate, benign trade-off of this codebase's pool-based no-GC allocator: pooled memory is reclaimed by the OS at process exit rather than individually freed, which LeakSanitizer's exit-time check isn't well-suited to distinguish from a genuine leak) — none of the leak stack traces ever touched `perl_closure_release`/`make_code_ref_impl`/capture-related code. A `-DPERL_ALLOC_DEBUG` secondary pass reported zero unfreed PVs. **New, separate, pre-existing defect confirmed while writing this fix's own tests, NOT fixed here** — logged as new **D64**: a captured scalar that compiles to perlc's unboxed int/float fast path (`intScopes_`/`floatScopes_` — a plain `my $x = 0;` at *block* scope, not file scope, is eligible) gets a *boxed snapshot* of its current value at capture time (`boxI64`/`boxF64`) rather than a share of the real stable-pointer storage, since the unboxed fast path has no `PerlValue*` to share in the first place — confirmed this affects `AnonSub` and `sort{}`'s comparator identically (`my $calls = 0;` inside a block, captured by a `sort { $calls++; ... }` comparator, silently stays `0`; the file-scope form of the identical code works correctly, since file-scope int/float variables always route through `fileScalarGlobals_`, a real PV-backed global, never the unboxed fast path). This fix's own test suite works around it throughout by initializing every mutated captured scalar as a string (`"0"`, not `0`) — not a workaround specific to this fix, but a pre-existing, orthogonal codegen-level gap this runtime-level refcounting change cannot address (no amount of reference-counting fixes "the wrong thing got captured in the first place"). Tests: `tests/closure_capture_mutation_smoke.pl` (1 check) and `tests/closure_capture_mutation.pl` (13 checks across 7 sections: same-scope mutation, two-way visibility, the adversarial escaping-closure crash repro, multiple independent closures, sort comparator scalar mutation, repeated-sort capture reuse, and a no-mutation regression) — verified byte-for-byte against real Perl. Full regression suite: 99/106 pass (up from 97/104 — 2 new test files added and both passing, the same 7 pre-existing failures unchanged, no new regressions). |
| D64 | A captured scalar that compiles to perlc's unboxed int/float fast path (block-scoped `my $x = 0;`) gets a boxed *snapshot* at capture time instead of sharing the real storage — mutations from inside the closure/comparator never propagate, identically for `AnonSub` and `sort{}`'s comparator | **FIXED (2026-07-14)** | Found while writing D62's fix tests. `{ my $x = 0; my $f = sub { $x++; }; $f->(); $f->(); print $x; }` printed `0` under perlc instead of real Perl's `2` — confirmed unrelated to D62 (D62's fix, fully landed, did not change this) and unrelated to `sort` specifically (reproduced identically for a plain `AnonSub`, and separately for `{ my $calls = 0; my @s = sort { $calls++; $a<=>$b } @data; }`, where `$calls` stayed `0`). Root cause: a block-scoped `my $x = <int/float literal>` is eligible for perlc's unboxed fast-path optimization (`intScopes_`/`floatScopes_`, a raw `i64`/`double` alloca with no `PerlValue*` at all, used for fast arithmetic), so when the existing closure-capture collection logic (`AnonSub`'s Phase 1, and the identical pattern D61 added to sort's comparator) fell back to `lookupIntVar`/`lookupFloatVar` for a name not found via `lookupVar`, it boxed the *current* value into a fresh, one-off `PerlValue*` (`boxI64`/`boxF64`) purely to have something capturable — structurally incapable of aliasing back to the real unboxed storage, since that storage was never a `PerlValue*` to begin with. A file-scope `my $x = 0;` was unaffected — file-scope int/float variables always route through `fileScalarGlobals_` (a real, PV-backed `GlobalVariable`), never the unboxed fast path. Fixed by making the fast-path *declaration* itself refuse the optimization for any name provably captured somewhere in the current function: a new `collectClosureCapturedNames()` (codegen.cpp, next to the existing `collectAllScalarNames()`) walks a function body once, collecting every scalar name referenced inside any `AnonSub`/`sort{}`-custom-comparator body found anywhere within it (deliberately over-approximate — a name merely reused, unrelated, elsewhere in the same function still loses the fast path, trading a rare missed optimization for guaranteed correctness). Computed once at the entry of each of the three places a function body is compiled — `emitSub` (named subs), `AnonSub`'s own Phase 2 (so a nested closure's *own* locals get the same treatment), and both entry points of the top-level program body (`compile()`'s normal-`main` path and its `--do-lib` path) — stored in a new member, `capturedNamesInCurrentFn_`, saved/restored around each nested compilation exactly like the existing `currentSubNeedsWantarray_`/`currentPackage_` pattern. `case NK::My:`'s existing fast-path condition (codegen.cpp) gained one extra clause requiring the declared name not be in that set; when it is, the declaration falls through to the normal, real-`PerlValue*`-backed path, exactly as a string-initialized variable already does. Verified against real Perl: `AnonSub` int and float captures, `sort{}` comparator int capture, a closure nested two levels deep still correctly reaches the outer int-fast-path variable, two-way visibility (an outside mutation after closure creation is seen inside the closure), the identical variable name captured in one function but not in an unrelated sibling function (each function's own independent analysis), and — as a regression check — a non-captured int-fast-path variable in a tight loop is completely unaffected (confirmed via a direct N-body-benchmark timing comparison before/after this fix showing no measurable difference, since the added analysis is a one-time, compile-time-only AST scan with zero runtime cost). `make test-tsan`: `perlc_tsan` (the TSan-*instrumented compiler binary*, not the generated programs) began reliably SEGV-ing during its own `CodeGen::~CodeGen()` destructor cleanup once this fix's changes were present (2/2 runs with vs. 0/2 without, confirmed via `git stash`) — but this is the already-documented D54 compiler-process artifact surfacing with a different symptom (crash instead of hang) than before, not a new thread-safety bug: the crash site (LLVM `Function*` hashtable teardown) has no connection to this fix's actual logic (a pure AST-walk with no signals/forking/threading involved), and D54's own notes already establish that unrelated code changes can newly expose or suppress this latent, probabilistic TSan+fork race by shifting the compiler binary's memory layout/timing. `TSAN_OPTIONS=die_after_fork=0` (D54's own documented workaround) got past it for all three TSan test files, each then running and passing every internal self-check with zero actual TSan race warnings — confirming no real thread-safety regression, consistent with D54's established precedent of verifying correctness via the regular non-TSan `tests/harness.sh` when `perlc_tsan` itself is flaky. Tests: `tests/closure_unboxed_capture_smoke.pl` (1 check) and `tests/closure_unboxed_capture.pl` (9 checks across 7 sections: `AnonSub` int/float capture, sort comparator int capture, an uncaptured-fast-path regression, the same name captured-vs-uncaptured in sibling functions, two-way visibility, and a nested-closure capture) — verified byte-for-byte against real Perl. Full regression suite: 101/108 pass (up from 99/106 — 2 new test files added and both passing, the same 7 pre-existing failures unchanged, no new regressions). |

### Low / Cosmetic Defects

| Defect ID | Problem | Status | Notes |
|-----------|---------|--------|-------|
| D31 | Float-to-string formatting uses C's default `%g` (6 significant digits) instead of Perl's `%.15g` | **FIXED (2026-07-11)** | `perl_to_string()` and `perl_to_string_dup()` (runtime.c) both formatted `PERL_FLOAT` values with plain `%g`. Fixed by adding a shared `perl_format_float()` helper using `%.15g`, plus three special cases where raw `%.15g` output differs from real Perl: negative zero (`%.15g` → `"-0"`, Perl → `"0"`), +-infinity (`%.15g` → `"inf"`/`"-inf"`, Perl → `"Inf"`/`"-Inf"`), and NaN (glibc's `%.15g` can render a signed-bit NaN as `"-nan"`, Perl always → `"NaN"`). This fix also resolved `tests/arith.pl`'s pre-existing harness failure as a side effect. Tests: `tests/float_stringify_smoke.pl` (3 checks) and `tests/float_stringify.pl` (18 checks across 7 sections: full-precision division/sqrt/literals, short-float regression, large/small exponential notation, negative zero, +-infinity, NaN, interpolation/concatenation contexts) — verified byte-for-byte against real Perl. Full regression suite improved from 68/77 to 71/79 (2 new test files, `arith.pl` newly passing, remaining pre-existing failures unchanged, no new regressions). |
| D32 | `$.` (input line number) is not reset to 0 when its filehandle is closed | **FIXED (2026-07-11)** | `perl_close_fh()` (runtime.c) freed/reset the filehandle's own `PerlValue` but never touched `s_dollar_dot`. Fixed by setting `s_dollar_dot.ival = 0` there, matching real Perl's `close()`. |
| D33 | `Scalar::Util::looks_like_number` returns integer `0` for false instead of Perl's empty string `""` | **FIXED (2026-07-11)** | `perl_su_looks_like_number()` (runtime.c) returned `perl_alloc_int(0)` on every false path. Fixed by changing all of those to `perl_alloc_string("")`, matching real Perl (the true case, `1`, was already correct). This fix also resolved `tests/tier2.pl`'s pre-existing harness failure (it exercises `looks_like_number` directly). |
| D43 | `wantarray()` called at top level (outside any sub) returns `0` instead of `undef` | **FIXED (2026-07-11)** | `perl_wantarray()` (runtime.c) unconditionally read `s_wantarray_stack[s_wantarray_depth - 1]` — when called outside any sub (`s_wantarray_depth == 0`), this is `s_wantarray_stack[-1]`, an out-of-bounds/undefined-behavior read (didn't crash in practice, but was reading memory before the array). Fixed by checking `s_wantarray_depth <= 0` first and returning `perl_alloc_undef()` in that case, matching real Perl and eliminating the out-of-bounds read as a side effect. |
| — | No `use warnings` diagnostic system exists at all | **OPEN** (completeness, not correctness) | Explains 2 of the 13 `harness.sh` diffs (`advanced.pl`, `fileio.pl` — both differ from real Perl only in the absence of stderr warning lines perlc never emits). Affects debuggability of every perlc-compiled program, not just these two tests. |

### Newly Discovered Defects (2026-07-14 comprehensive re-review)

Found via a 4-way parallel review (empirical re-verification of every prior OPEN defect, a fresh docs-vs-source audit, a broad new-corner probe of string/numeric/list/regex/OOP edge cases, and a full `tests/harness.sh` rerun). The harness rerun itself found **zero regressions** — the same 7 files diverge from real Perl as before (`advanced.pl`, `eval_string.pl`, `fileio.pl`, `mbs.pl`, `regex_named.pl`, `tier1.pl`, `wantarray_extended.pl`), all already explained by existing registry entries. Everything below is new. IDs continue from D66.

| Defect ID | Problem | Status | Notes |
|-----------|---------|--------|-------|
| D67 | `pack`/`unpack` are completely non-functional despite being documented as a fully-shipped feature | **FIXED (2026-07-14) for all non-NUL-byte-producing format/value combinations — see D85 for the remaining, separate, architectural gap** | The parser built `NK::PackFunc`/`NK::UnpackFunc` nodes and `runtime.c` implemented `perl_pack`/`perl_unpack`/`perl_unpack_to_array`, but `codegen.cpp` had **zero `case` for either node kind** — confirmed via `grep -n "NK::PackFunc\|NK::UnpackFunc" src/codegen.cpp` returning no hits. Every `pack(...)`/`unpack(...)` call silently compiled to a no-op evaluating to empty/undef, with no error at compile or run time. **Fixed** by adding the missing codegen wiring: `emitExpr`'s `NK::PackFunc` (builds a temp `PerlArray` from the value args, calling `perl_pack`) and `NK::UnpackFunc` (scalar context, calling `perl_unpack`) cases, plus `emitArrayPtr`'s `NK::UnpackFunc` case (list context, calling `perl_unpack_to_array`) — mirroring the existing `SprintfFunc`/`JoinFunc`/`SplitFunc` dispatch patterns. Three RT() runtime-function registrations (`perl_pack`, `perl_unpack`, `perl_unpack_to_array`) were also missing entirely and had to be added. **Several additional, real bugs in the runtime functions themselves were found and fixed while testing this wiring** — the functions existed but had never actually been exercised end-to-end before (nothing could reach them), so they carried their own latent defects: (1) `*` as a count specifier (`unpack("C*",...)`, `pack("C*",@vals)` — an extremely common idiom) was not recognized at all by either `perl_pack` or `perl_unpack_to_array`, silently defaulting to a count of 1 (only the first value/byte); now correctly means "all remaining args" for pack's numeric types, "as many whole elements as fit in the remaining bytes" for unpack's numeric types, and "this one arg's own length" / "all remaining bytes" respectively for the `a/A/h/H/b/B` string types. (2) `perl_pack`'s `a`/`A`/`h`/`H`/`b`/`B` handling treated their count as "repeat over that many separate args" (matching the numeric types' semantics) instead of real Perl's actual rule — a FIELD WIDTH applied to a single arg, padded with NUL (`a`) or spaces (`A`) if short, truncated if long (confirmed: `pack("A5","hi")` gave `"hi"` unchanged instead of real Perl's space-padded `"hi   "`; `pack("A2","hello")` gave the whole `"hello"` instead of truncating to `"he"`). (3) `perl_unpack_to_array`'s `A` didn't strip trailing spaces/NUL bytes from the extracted field the way real Perl's ASCII-string unpack does (confirmed `unpack("A5","hi   ")` gave `"hi   "` instead of `"hi"`); `perl_unpack`'s scalar-context path was also found to return `perl_ref_array(result)` — an ARRAY *reference* — instead of a single scalar value, likely never previously caught since nothing could call it. (4) `PackFunc`'s new codegen didn't originally flatten array-valued arguments (`pack("C4", @arr)` needs `@arr`'s 4 elements individually) — an array argument passed through plain `emitExpr()` gives just its scalar-context element count instead; fixed the same way `JoinFunc` already handles this, via `emitArrayPtr`-based flattening with a scalar fallback. `perl_unpack`'s previously-separate, ~120-line duplicate copy of the entire format-parsing loop (with its own, independently-drifted bugs — the wrong-return-type issue above, and a missing `*`/`A`-stripping fix that would otherwise have needed a second, separate patch) was deleted; it now delegates to `perl_unpack_to_array` and takes just the first element, so there is exactly one unpack-format-parsing implementation. **Remaining, deliberately NOT fixed here** (out of a single defect's reasonable scope): `h`/`H`/`b`/`B` (hex/bit-string formats) still just copy raw bytes rather than doing real nibble/bit-level encoding — narrower and much less commonly used than the fixes above; and, most significantly, **any packed data containing an embedded NUL byte is silently truncated** — logged separately as **D85**, a deep architectural limitation of `PerlValue`'s NUL-terminated (no explicit length) string representation, not something fixable within this defect's scope. Verified directly against real Perl: all documented numeric format codes (`c/C/s/S/n/N/v/V/l/L/i/I/q/Q`) with values chosen to avoid embedded NUL bytes, `a`/`A` field-width padding and truncation (encode) and trailing-strip (decode), `*` for both string and numeric types on both pack and unpack, array-argument flattening, multiple format codes combined in one call, round-tripping pack→unpack, and both scalar and list unpack context. Tests: `tests/d67_pack_unpack_smoke.pl` and `tests/d67_pack_unpack.pl` — see their own headers for why NUL-byte-producing patterns are deliberately excluded (D85, not this fix). Full `tests/harness.sh` regression suite re-run after the fix with zero new failures. |
| D68 | `substr` is not actually UTF-8 aware, only `length` is — contradicts CLAUDE.md's "UTF-8 aware length/substr" claim | **OPEN — HIGH** | `length()` on a string containing a multi-byte char (e.g. built via `chr(233)`) correctly returns character count, but `substr($s,1,1)` on that same string returns a garbled byte instead of the character, and `ord(substr($s,1,1))` returns `0` instead of `233`. `length` is character-aware while `substr` is still byte-oriented — an internally inconsistent implementation, not simply "UTF-8 unsupported." |
| D69 | `List::Util::uniq` is broken on its two most common call shapes | **FIXED (2026-07-15)** | `uniq(@array)` (array-variable argument, the typical call shape) did not deduplicate at all — returned all elements unchanged. `List::Util::uniq(...)` (fully-qualified) returned an empty list. Only `uniq(1,1,2,2,3)` (bare name + literal list) worked correctly. **Three distinct root causes, all fixed**: (1) `perl_uniq_list()` (runtime.c) only stripped *consecutive* duplicate elements (Unix `uniq`-command semantics, matching its own stale doc comment "remove consecutive dups"), not real Perl's actual behavior of keeping the first occurrence of each distinct value *anywhere* in the list — confirmed via `--emit-ir` that codegen already correctly passed `@array`'s own `PerlArray*` straight into this function, so the bug was entirely inside it; `(3,1,4,1,5,9,1)` has no adjacent duplicates, so it passed through completely unchanged, while `(1,1,2,2,3)` happened to have every duplicate adjacent, masking the bug. Fixed by rewriting it to use a hash-based "seen" set (keyed by each element's stringification, matching how real Perl's `uniq` compares values) instead of only comparing to the immediately preceding element. (2) `uniq`/`sum`/`min`/`max` are recognized only via a dedicated lexer keyword for the *bare* identifier; a qualified reference like `List::Util::uniq` doesn't match that keyword check, so it parsed as an ordinary `NK::Call` with no special-casing anywhere (unlike `POSIX::floor`/`ceil`/`fmod`, which already had this exact "qualified name" interception pattern in the same file) and fell through to the generic qualified-call codegen, which only knows how to invoke a real, separately-compiled LLVM sub — since none of these are that, the call silently produced nothing. Fixed by adding explicit `"List::Util::sum"`/`"min"`/`"max"`/`"uniq"` name interceptions in both `CodeGen::emitCall` (scalar context) and `CodeGen::emitArrayPtr` (list context, `uniq` only — `sum`/`min`/`max` are always scalar), mirroring the existing `POSIX::floor`/`Time::HiRes::gettimeofday` precedent. (3) Found while writing this fix's own tests, a third, smaller, separate bug sharing the same code paths: real `List::Util::uniq` in scalar context returns the *count* of unique elements, not the first element's value — perlc's pre-existing code (and this fix's own first draft, copied from it) did the latter; fixed by changing `perl_array_get(uniq_list,0)` to `perl_array_len(uniq_list)` at both the bare-keyword and newly-added qualified-name call sites. Verified directly against real Perl: non-consecutive and consecutive duplicates, already-unique/empty/single-element lists, string (not just numeric) values, scalar-context count for both literal-list and array-variable arguments, the fully-qualified form in both list and scalar context, fully-qualified `sum`/`min`/`max` array-argument flattening, and bare `sum`/`min`/`max` regressions. Tests: `tests/d69_uniq_smoke.pl` (2 checks) and `tests/d69_uniq.pl` (18+ checks across 8 sections) — verified byte-for-byte against real Perl. Full `tests/harness.sh` regression suite re-run after the fix with zero new failures. |
| D70 | `POSIX::fmod` silently returns `undef` when imported via `use POSIX qw(fmod)` and called unqualified — only the fully-qualified form works | **OPEN — MEDIUM** | `use POSIX qw(fmod); fmod(10,3)` → empty/undef every time; `POSIX::fmod(10,3)` → correct (`1`). `tests/tier2.pl` passes only because it happens to call the fully-qualified form, masking this gap from the harness. |
| D71 | `%` (modulo) gives the wrong result whenever either operand is negative | **FIXED (2026-07-14)** | perlc used C's truncating modulo instead of Perl's floored-division convention; only `positive % positive` was correct. `-7 % 3` → perl `2`, perlc `-1`. `7 % -3` → perl `-2`, perlc `1`. A basic arithmetic operator, silently wrong across the entire negative-operand input space — affected any code using `%` for circular indexing, hashing, or wraparound with negative values. **Fixed** at all three call sites: `runtime.c`'s `perl_mod()` (the general boxed-value path, used at file scope and for non-fast-path expressions) now computes the C truncating remainder and adds the divisor back when its sign doesn't match a nonzero result's sign (the standard truncated→floored conversion); `codegen.cpp` gained a shared `CodeGen::emitFlooredMod()` helper doing the branchless LLVM-IR equivalent (`SRem` + sign-mismatch `select`), used by both places that previously called `CreateSRem` directly — the unboxed-int fast path for plain `%` (`emitExprI64`'s `NK::BinOp` case) and for compound `%=` (the int-var compound-assignment fast path). Verified: all sign combinations (`+/+`, `-/+`, `+/-`, `-/-`), exact multiples, zero dividend, modulus of ±1, variables (not just literals), the `%=` compound-assign fast path specifically, a loop re-executing the fast path repeatedly, large-magnitude operands, float operands (Perl truncates to int first), and the circular-array-indexing idiom that motivated this fix's priority ranking. **New, separate, pre-existing defect found while writing this fix's regression test, logged as D84, NOT fixed here**: `%`/`%=` by zero is not a catchable Perl exception (the unboxed-int fast path has no zero-divisor check at all; the boxed path's existing check calls `exit(1)` directly instead of routing through `perl_die()`, the same non-catchable-exit pattern D35 already fixed for `Carp::croak`). Tests: `tests/d71_modulo_negative_smoke.pl` (3 checks) and `tests/d71_modulo_negative.pl` (20+ checks across 10 sections) — verified byte-for-byte against real Perl. Full `tests/harness.sh` regression suite re-run after the fix with zero new failures. |
| D72 | `"$ref->{key}"` / `"$ref->[i]"` (arrow dereference) does not interpolate inside double-quoted strings | **FIXED (2026-07-14)** | `"elem: $href->{a}\n"` printed `"elem: HASH(0x...)->{a}"` instead of the value; `"elem: $aref->[1]\n"` printed `"elem: ->[1]"` (the array's own interpolation swallowed `$aref`, leaving a dangling `->[1]`). One of the single most common Perl OOP idioms (`"Name: $self->{name}"`) silently produced garbage rather than an error. Root cause: `Parser::parseStringInterp`'s raw-character scanner recognized only a bare trailing `[idx]`/`{key}` after a variable name (plain `@var`/`%var` element access) — it had no notion of `->` at all, so the arrow and subscript fell through to the scanner's default literal-character path. **Fixed** by extending the scanner into a small loop that recognizes an explicit `->` immediately followed by `[` or `{` as the start of a dereference chain (building the same `ArrowDeref` AST nodes the token-based parser produces for non-interpolated code), and continues consuming further adjacent `[idx]`/`{key}` segments — with or without their own repeated `->` — as further chain links, mirroring real Perl's rule that `$ref->{a}{b}` and `$ref->{a}->{b}` are equivalent (only the first subscript needs an explicit arrow). An explicit `->` *not* followed by `[`/`{` (e.g. `"$obj->method"`) is deliberately left unconsumed via a one-token lookahead before committing to it, since real Perl's interpolation doesn't evaluate method/code-ref calls either — only `->[`/`->{` chains. **One implementation bug caught during testing, not shipped**: an early version of the fix forgot to seed the chain's accumulator node with the plain scalar-var node before the loop, so an *immediate* leading arrow (`$ref->{key}`, arrow on the very first subscript) moved a null `unique_ptr`, hitting an assertion failure whose crash-handler hang looked like an infinite loop under a timeout — caught by testing the exact repro before considering the fix done, fixed by initializing the accumulator upfront. Tests: `tests/d72_arrow_interp_smoke.pl` (2 checks) and `tests/d72_arrow_interp.pl` (15+ checks across 9 sections: basic hash/array arrow, chained subscripts with/without repeated `->`, array-of-hashrefs, array-of-arrays, triple-nested chains, variable index/key inside a chain, negative array index, end-of-string, the `->method`-stays-literal case, and regressions for plain `$hash{key}`/`$arr[i]`/`${x}`/bare-scalar interpolation) — verified byte-for-byte against real Perl. Full `tests/harness.sh` regression suite re-run after the fix with zero new failures. |
| D73 | Array/hash slices do not interpolate inside double-quoted strings | **FIXED (2026-07-14)** | `"@arr[1,2]"` printed the whole array's space-joined stringification followed by the literal subscript text (`"10 20 30 40[1,2]"`) instead of just the sliced elements (`"20 30"`); `"@h{'a','b'}"` similarly printed garbage instead of `"1 2"`. Root cause: `Parser::parseStringInterp`'s `"@arr"` scanning block unconditionally built a whole-array join as soon as it saw `@name` — it never checked whether a `[` or `{` immediately followed the name. Fixed by extending the same block: after the variable name is read, check for an immediately-following `[`/`{`; if present, extract the bracketed content (tracking same-type bracket nesting depth), parse it as a comma-separated expression list via a small new `Parser::parseExprListFromTokens` helper (added alongside the pre-existing single-expression `parseExprFromTokens`), and build an `ArraySlice`/`HashSlice` node — the same node kinds the token-based parser already produces for non-interpolated `@arr[LIST]`/`@hash{LIST}` syntax, and which `emitArrayPtr`'s existing `ArraySlice`/`HashSlice` cases already handle correctly. Either the slice or the pre-existing whole-array node is wrapped in the same space-joining `JoinFunc` as before. Verified directly against real Perl: single-element and 2/3-element slices, non-contiguous indices, negative indices, variable index/key inside the slice, a slice embedded in surrounding literal text, and regressions for whole-array interpolation, plain `$hash{key}`/`$arr[i]` interpolation, and array-of-arrays subscripting (not slice syntax, confirmed not confused with it). Tests: `tests/d73_slice_interp_smoke.pl` (2 checks) and `tests/d73_slice_interp.pl` (13+ checks across 8 sections) — the deep test deliberately omits `use warnings` (real Perl's harmless "Scalar value @arr[1] better written as $arr[1]" stylistic warning on single-index slices, which the fix must still interpolate correctly despite the discouraged style, would otherwise break the exact-match comparison — same precedent as other files in this suite, e.g. `sort_scalar_context.pl` for D29). Full `tests/harness.sh` regression suite re-run after the fix with zero new failures. |
| D74 | `substr($str, $off, $len, $replacement)` — the 4-argument in-place-replacement form — is a silent no-op | **FIXED (2026-07-14)** | `substr($u, 1, 3, "XYZ")` left `$u` completely unchanged instead of splicing in the replacement (real Perl: `"Hello"` → `"HXYZo"`). The 3-arg lvalue-assignment form (`substr(...) = X`) already worked correctly — only the 4-arg form was broken, and it failed silently (no error, no effect) rather than either working or refusing to compile. Root cause: the parser already accepted up to 4 comma-separated `substr` arguments, but `CodeGen::emitExpr`'s `NK::SubstrFunc` case only ever checked `args.size() >= 3` — there was no branch at all for a 4th argument, so the call compiled and ran identically to the 3-arg read-only form, with the replacement argument evaluated for nothing (or not at all) and silently discarded. **Fixed** by adding an `args.size() >= 4` branch: it computes the OLD substring value first via the same `perl_substr3` runtime call the 3-arg form already uses (real Perl's documented 4-arg return value is the *old*, pre-replacement substring), then performs the actual in-place mutation via `perl_substr_replace` — the same runtime function the pre-existing 3-arg lvalue-assignment form already used, reusing its "stable pointer" reliance (a plain scalar-variable expression's `emitExpr` result is the variable's own storage, not a clone, so mutating it in place correctly reaches back into the original variable). Verified directly against real Perl: the original repro, the 4-arg return value being the old substring, replacements shorter/longer/equal-length/empty (deletion), negative offsets, replacing to the end of the string, multiple sequential replacements on the same variable, and regressions for the 3-arg lvalue-assignment, 3-arg read-only, and 2-arg read-only forms (none mutate the string when they shouldn't, and all still produce the correct extracted value). **New, separate, pre-existing defect confirmed while testing, NOT introduced by this fix**: negative-length `substr` (`substr($s, $off, -$n, ...)`) reproduces the already-logged D77 (substr negative-length/far-negative-start mishandled) identically in the plain 3-arg *read-only* form too — this fix reuses the exact same bounds-calculation logic the read-only form already had (`substr_bounds_utf8`), so it inherits D77's pre-existing bug rather than introducing a new one; negative-length cases are deliberately excluded from this fix's own tests, since D77 tracks that gap separately. Tests: `tests/d74_substr_4arg_smoke.pl` (1 check) and `tests/d74_substr_4arg.pl` (12+ checks across 10 sections) — verified byte-for-byte against real Perl. Full `tests/harness.sh` regression suite re-run after the fix with zero new failures. |
| D75 | Multiple-inheritance method resolution picks the wrong method — doesn't follow Perl's default depth-first left-to-right `@ISA` search order | **FIXED (2026-07-15)** | `package D; our @ISA=('B','C');` where both `B` and `C` (and their own parent `A`) define `hello`: real Perl's default DFS resolves to `B`'s (and `B`'s own `SUPER::` correctly walks up to `A`), perlc instead resolved to `C`'s method. Single-parent `SUPER::` chains (verified 3 levels deep) were already correct — only the multi-parent dispatch order was wrong. Root cause: `perl_set_isa(child, parent)` (runtime.c) "updated if already registered" — overwriting any existing entry for `child` in place instead of adding a new one. `our @ISA = ('B','C')` calls this once per @ISA element in order (codegen.cpp already did this correctly), so for a multiply-inherited class only the LAST call's parent ever survived in the ISA table — a class with 2+ parents could only ever see the last-registered one. `perl_find_method`/`perl_isa_check` (backing `->method`/`->isa()`) then only ever walked that single remembered parent as a plain linear chain, never a true multi-parent tree. **Fixed** by making `perl_set_isa` always append a new `(child,parent)` entry instead of overwriting; adding a `perl_isa_direct_parents()` helper that collects ALL of a class's registered parents in `@ISA` order; and rewriting `perl_find_method` (method dispatch), `perl_isa_check` (`->isa()`), and `perl_dispatch_method_super` (`SUPER::`) to do a genuine depth-first search over the full `@ISA` tree — check the class itself, then recursively exhaust each direct parent's entire subtree in left-to-right order before trying the next parent — matching real Perl's actual default (non-C3) MRO exactly, including its well-known "diamond problem" quirk (a shared grandparent method reached via a non-overriding first branch wins over an overriding second branch — confirmed this exact, sometimes-surprising-but-correct behavior is reproduced byte-for-byte, not "improved" on). `perl_can_check` (`->can()`) needed no separate fix — it already delegated to `perl_find_method`. Verified directly against real Perl: the original repro (DFS dispatch with `SUPER::`); a method defined only in the second parent's subtree, still found; `->isa()`/`->can()` recognizing both direct parents and a shared grandparent; a hard-negative case (`->isa()` correctly false for an unrelated class); plain single-inheritance 3-level-deep chains (regression); the diamond-inheritance DFS quirk; and three-parent (not just two) `@ISA`. Tests: `tests/d75_multi_inherit_smoke.pl` (2 checks) and `tests/d75_multi_inherit.pl` (20+ checks across 7 sections) — verified byte-for-byte against real Perl. Full `tests/harness.sh` regression suite re-run after the fix with zero new failures. |
| D76 | A blessed object's `DESTROY` does not cascade to blessed objects it holds a sole reference to | **OPEN — MEDIUM** | An `Outer` object holding an `Inner` blessed object as a hash value: when the `Outer` instance goes out of scope, `Outer::DESTROY` fires but `Inner::DESTROY` never does (real Perl fires both, inner after outer). A silent resource-cleanup gap — e.g. a wrapper object holding a filehandle-owning inner object would leak the inner resource. |
| D77 | `substr` mishandles negative length and far-out-of-range negative start | **FIXED (2026-07-15)** | `substr($s, 2, -3)` (negative length means "stop N chars before the end" in real Perl) was instead treated as if no truncation occurred, returning too much. `substr($s, -100, 3)` (start further negative than the string is long) should return `undef` in real Perl (a completely out-of-range window); perlc silently returned an empty string instead (the original bug description's guess that it "should clamp to the start" was itself wrong — confirmed via direct testing that real Perl actually returns `undef`, not a clamped result, for a window that doesn't overlap the string at all). **Root cause and fix**: `substr_bounds_utf8()` (runtime.c) had two bugs — it unconditionally replaced a negative `n` (length) with `slen - off` ("go to the end", i.e. treating negative length as "no length limit" rather than its real meaning), and it always clamped an out-of-range `off` into `[0,slen]` with no way to signal "this doesn't overlap the string at all", so the read forms could never return `undef` and the 4-arg replace/lvalue form could never die, regardless of how nonsensical the input was. Reverse-engineered real Perl's actual algorithm empirically (a 1012-case matrix: offset -15..15 × length -15..15 on a 10-char string, plus a separate 51-case offset-only sweep for the 2-arg form — both matched the fix 100%): `start = OFFSET` (adjusted by `+= slen` if negative); if `start > slen`, out of range. `raw_end = slen` (2-arg form, no LENGTH given) or `start + LENGTH` (LENGTH ≥ 0) or `slen + LENGTH` (LENGTH < 0, "N chars back from the end"). If `start < 0` after the offset adjustment: out of range if `raw_end < 0` too (window entirely before the string), otherwise clip `start` to `0` (partial overlap). Finally clamp `raw_end` to `[start, slen]`. `substr_bounds_utf8()` now returns a bool (in-range or not) instead of unconditionally succeeding; `perl_substr2`/`perl_substr3` return `perl_alloc_undef()` on out-of-range (matching real Perl's read-form `undef`+warning behavior — the warning itself isn't emitted, since perlc has no `use warnings` diagnostic system at all, D56, a separate pre-existing gap); `perl_substr_replace` (the D74 4-arg form) instead calls `perl_die()` with `"substr outside of string"` on out-of-range, leaving the target string unmodified — matching real Perl's actual (fatal, catchable-via-`eval`) behavior for that form specifically, confirmed by direct testing. Verified byte-for-byte against real Perl across the full derived matrix (negative length at various offsets including exact-zero-result and far-overshoot cases; negative offset both in-range/partial-overlap and entirely-out-of-range/`undef`; the exact boundary between "empty but defined" and `undef`; positive offset beyond the string; the 2-arg form's asymmetric behavior — negative offset always clamps to the full string and never returns `undef`, unlike the 3-arg form, since an absent LENGTH behaves as "infinite"; a very negative offset rescued by a sufficiently large positive length; the 4-arg replace form for both negative length and complete out-of-range dying; and regressions for ordinary in-range substr in both read and replace forms). Tests: `tests/d77_substr_negative_smoke.pl` (1 check) and `tests/d77_substr_negative.pl` (31 checks across 11 sections) — verified byte-for-byte against real Perl. Full `tests/harness.sh` regression suite re-run after the fix with zero new failures. |
| D78 | Signed integer overflow wraps around instead of auto-promoting to float | **OPEN — MEDIUM** | `9223372036854775807 + 1` → real Perl `9223372036854775808` (auto-promotes to double on overflow); perlc → `-9223372036854775808` (raw two's-complement wraparound, silently wrong sign and magnitude). |
| D79 | String-to-number coercion incorrectly auto-detects a `"0x..."` prefix as hexadecimal | **OPEN — LOW-MEDIUM** | `"0x10" + 1` → real Perl `1` (implicit string→number conversion stops at the first non-digit, `"0"`, giving numeric value `0`); perlc → `17` (parses the whole `"0x10"` as hex). Only explicit `hex()`/`oct()` should do base auto-detection in real Perl; implicit coercion never does. |
| D80 | `$+{name}` (named regex capture) does not interpolate inside double-quoted strings | **OPEN — LOW-MEDIUM** | `"$+{year}-$+{month}"` prints literally (un-interpolated) instead of the captured values. The bare expression form (`print $+{year}`) works correctly — only string interpolation of this specific hash fails. |
| D81 | `delete @hash{...}` / `delete @arr[...]` (slice-form `delete`) is a hard parse error | **OPEN — LOW-MEDIUM** | `delete @d{qw(a b)}` → `Error: Parse error: expected $ but got '@'`. Single-key `delete $h{k}` works fine; the parser has no production for a slice base under `delete`. Blocks otherwise-valid code rather than silently misbehaving. |
| D82 | `sprintf`/`printf` positional argument specifiers (`%N$s`) are not supported | **OPEN — LOW** | `printf("%3\$s %1\$s\n", "a","b","c")` → real Perl `"c a"`; perlc consumes args positionally in order regardless of the index, giving `"%s %s"` worth of wrong/garbled substitution. Niche but silent. |
| D83 | `ref()` returns empty string instead of `"ARRAY"` for a scalar holding a FLAT_ARRAY-tagged value | **OPEN — MEDIUM** | Found 2026-07-14 while writing D66's regression test. `my $y = [4,5,6]; print ref($y);` → real Perl `ARRAY`; perlc → empty string. Reproduces for any all-numeric, ≥2-element anonymous array literal assigned directly to a scalar (the FLAT_ARRAY fast-path optimization described in CLAUDE.md's Architecture section) — confirmed unrelated to hash elements specifically (reproduces with no hash involved at all) and unrelated to D66 (reproduces identically with or without D66's fix applied, verified directly via a targeted before/after rebuild). `\@named_array` (producing a plain `REF_ARRAY`-tagged value, not FLAT_ARRAY) and a mixed-type literal like `[1,"two",3]` (which doesn't qualify for the FLAT_ARRAY path) both correctly return `"ARRAY"` — only the FLAT_ARRAY-tagged case is broken. `ref()`'s runtime implementation presumably checks for `PERL_REF_ARRAY` specifically and has no case for tag 10 (FLAT_ARRAY). Not yet root-caused in `runtime.c`/`codegen.cpp`; dereferencing the value itself (`$y->[i]`) still works correctly — only the `ref()` type query is wrong. |
| D84 | `%`/`%=` by zero is not a catchable Perl exception, and the unboxed-int fast path skips the zero-divisor check entirely | **OPEN — MEDIUM** | Found 2026-07-14 while writing D71's regression test. Two distinct sub-bugs sharing one root cause family: **(a)** inside any block/sub scope, `$a % $b` with int-var operands routes through the unboxed-int LLVM fast path (`CodeGen::emitFlooredMod`/formerly raw `CreateSRem`), which has **no zero-divisor check at all** — `eval { my $z = 5 % 0; }; print $@ ? "died" : "no-die";` prints `no-die` under perlc (real Perl: `died`), and the constant-folded case silently continues with an undefined/garbage result rather than crashing or dying. **(b)** At true file scope (where the fast path is gated off via `!atFileScope` and the call goes through the boxed `perl_mod()` runtime path instead), the existing zero check *does* fire (`fprintf(stderr,"Illegal modulus zero\n"); exit(1);`) but is **not eval-catchable** — it calls `exit(1)` directly instead of routing through `perl_die()`'s `setjmp`/`longjmp` mechanism, the identical non-catchable-exit anti-pattern D35 already fixed for `Carp::croak`, just never applied to `perl_mod` (or, unverified but likely by the same code shape, `perl_div`'s zero check). Not fixed here — out of scope for D71, which is specifically about the sign convention of a well-defined (nonzero-divisor) modulo, not this separate exception-handling gap. |
| D85 | `pack`/`unpack`'d binary data containing an embedded NUL byte is silently truncated at the first NUL — a deep, architectural limitation, not a targeted bug | **FIXED (2026-07-15)** | Found 2026-07-14 while fixing D67 (pack/unpack's missing codegen wiring). Root cause: `PerlValue`'s `PERL_STRING` representation (`char *sval`) was an ordinary NUL-terminated C string with no separate byte-length field — `perl_alloc_string()` called `strdup()`, and essentially every other string operation in `runtime.c` relied on `strlen()`/NUL-termination throughout, so `pack("N", 1234567)` (bytes `\x00\x12\xD6\x87`) silently lost everything after its leading `\x00` the moment it was wrapped into a `PerlValue`. **Fix (full architectural fix, explicitly chosen over a narrower pack/unpack-only mitigation)**: added an explicit `long long slen` byte-length field to `PerlValue` (`runtime.h`), authoritative for `PERL_STRING`-tagged values; `sval[slen]` is still always a NUL terminator for any remaining unconverted strlen()-based consumer (no worse off than before), but every length-aware consumer now sees the true byte count. `sizeof(PerlValue)` grew from 32 to 48 bytes — a `_pad_reserved` field and a new `_Static_assert(sizeof(PerlValue) % 16 == 0, ...)` keep it a 16-byte multiple, required because `pv_alloc()`'s slab (`calloc(PV_SLAB, sizeof(PerlValue))`) packs `PerlValue`s contiguously and the existing lock-free 16-byte CAS (`cmpxchg16b`/`ldxp+stxp`, used for `threads::shared` scalar RMW, itself allocated via the same slab) requires every entry to stay 16-byte aligned — a stride that wasn't a 16-byte multiple would have misaligned every other slab entry and silently broken or crashed that atomic path. Two new primitives anchor the fix: `perl_alloc_string_len(s, len)` (allocates exactly `len` bytes, may contain embedded NULs anywhere within) and `perl_to_string_dup_len(v, &out_len)` (length-aware counterpart to `perl_to_string_dup`). Threaded through roughly 30 call sites: `perl_alloc_string` (now a thin `strlen()`-based wrapper over `_len`), `perl_clone`, `perl_assign`, `perl_local_save` (dynamic-scope save/restore), `perl_is_true` (also fixed a related bug: a *genuine* 1-byte string holding just `chr(0)` was wrongly treated as falsy, indistinguishable from `""`, because the old check only looked at `sval[0]=='\0'` with no length gate), `perl_concat`/`perl_repeat_str` (`x`; rewritten from `strcpy`/`strcat` to length-tracked `memcpy`), `perl_str_eq/ne/lt/gt/le/ge` and `perl_str_spaceship` (`cmp`) plus the default sort comparators `cmp_str_pv`/`cmp_str_asc` (rewritten from `strcmp` to a new `perl_strcmp_len` — memcmp up to the shorter length, then break ties by length, matching Perl's byte-wise ordering), `perl_print`/`perl_say`/`perl_print_fh`/`perl_say_fh` (rewritten from `fputs` to `fwrite` of the true length), `perl_length` (via a new bounded `utf8_strlen_n`, since the existing `utf8_strlen` scanned until a NUL), `perl_chr_val` (`chr(0)` encoded correctly to a 1-byte NUL buffer but then re-truncated its own length via `strlen()` when wrapping it — used `utf8_encode`'s own returned length instead), `perl_pack`'s and `perl_sprintf`'s final output-buffer construction (both had an already-correctly-tracked byte position `pos` that was being discarded by wrapping the result in plain `perl_alloc_string()`), `perl_unpack_to_array` (both its own string-argument read and its `'a'`/`'A'` field-extraction push), `tr///` (made fully NUL-safe end-to-end — a pure byte-for-byte lookup-table mapping with no UTF-8 involvement, cheap to fix completely unlike substr/regex), and `perl_readline` (both slurp mode and line-delimited mode — the read half of the pack→file→unpack binary round trip; a raw `fread`-equivalent `fgetc` loop already tracked the exact byte count but discarded it the same way pack did). Also fixed `perl_read_fh` (Tier-3 `read($fh,$buf,$n,$offset)`) to use `slen` instead of `strlen()` for its append-at-offset case. **Several designated-initializer struct literals** (`PerlValue x = { .tag = PERL_STRING, .sval = ... }`, used for `$@`'s error message construction, `Carp::croak`'s bare "Died", etc.) needed an explicit `.slen = ...` added — a field a C designated initializer silently zero-inits if not listed, which combined with `perl_assign`'s new `slen`-driven copy would otherwise have *silently truncated non-empty literal messages to `""`* (a landmine that would have been a serious regression if missed; audited every such literal in the file). **Deliberately NOT fixed (out of scope, documented remaining gaps)**: `substr`/`chomp`/`chop`/magical string-increment still derive positions via `strlen()`/UTF-8 byte-scanning internally on their *source* string (not NUL-safe past an embedded NUL — `chomp`/`chop` were made length-aware for their own read/write of `v->slen` directly, but `substr`'s UTF-8 offset math was left as-is given the much larger scope of making `utf8_char_to_byte`/`utf8_strlen` themselves bounded); `sprintf`/`printf`'s `%s` conversion still truncates a NUL-containing *argument* at the inner `snprintf(..., "%s", ...)` call (a libc limitation — the *outer* buffer wrapping was fixed for every other conversion); PCRE2 regex matching against a NUL-containing *subject* string is still `strlen()`-bounded (pre-existing, not worsened). **New, separate, narrower defect found while writing this fix's regression test, logged as D90, NOT fixed here**: `utf8_strlen`'s byte-scanning heuristic (used by `length()`, unrelated to whether a string has the NUL-safety fix applied) can misidentify two consecutive raw bytes in binary data as a single UTF-8 continuation sequence — e.g. `pack("N", 1234567)`'s own bytes `\x00\x12\xD6\x87`: byte `0xD6` (`11010110`) matches a 2-byte UTF-8 lead pattern and the following `0x87` (`10000111`) matches a continuation-byte pattern, so `length()` undercounts by one (`3` instead of `4`) even though the byte *data* itself is now fully correct (confirmed via `unpack`). A pre-existing characteristic of `utf8_strlen` predating this fix (this fix's own `utf8_strlen_n` bounded variant faithfully preserves the same interpretation, just bounded instead of NUL-terminated) — genuinely fixing it would require tracking a real UTF8-flag per `PerlValue` (as real Perl does internally) to distinguish "this is UTF-8 text, decode as codepoints" from "this is raw binary, count bytes", which perlc has no representation for at all; a separate, large feature gap, not a targeted fix. **Two more unrelated, pre-existing gaps found and worked around (not fixed) while writing this fix's regression test**: `(EXPR)[i]` subscript on an `unpack()` list result returns an `ARRAY` ref instead of the scalar element at index `i` (worked around in the test with `my ($v) = unpack(...)` instead); and `my (undef, $x) = LIST` does not correctly skip the first list element (assigns the first element to `$x` instead of the second) — worked around with a throwaway named variable instead of a literal `undef` in the list-assignment LHS. Verified byte-for-byte against real Perl across: the original repro (pack/unpack round trip), concatenation, plain assignment, string `eq`/`ne`/`lt`/`cmp`, sort's default comparator, `tr///`, `is_true` for a lone NUL byte vs `""` vs `"0"`, `chr(0)` truthiness and length, passing a NUL-containing value through a sub call/array element/hash element, `local()` save/restore, a real file write-then-read round trip (binmode, slurp-mode readline), and regressions for every one of the above on plain ASCII data with no embedded NULs. `make test-tsan` re-run after the struct-size change: zero race reports across `threads.pl`/`threads_atomic.pl`/`destroy.pl`, confirming the 16-byte-alignment invariant holds. Tests: `tests/d85_nul_bytes_smoke.pl` (1 check) and `tests/d85_nul_bytes.pl` (28 checks across 12 sections). Full `tests/harness.sh` regression suite re-run after the fix with zero new failures (same 7 pre-existing). |
| D90 | `length()` undercounts by one for certain raw binary byte sequences that happen to look like a valid multi-byte UTF-8 continuation sequence (e.g. a byte matching the `110xxxxx` lead pattern immediately followed by one matching `10xxxxxx`) | **OPEN — LOW (narrow, cosmetic — byte *data* itself is correct)** | Found 2026-07-15 while writing D85's regression test: `pack("N", 1234567)` produces bytes `\x00\x12\xD6\x87`; `0xD6` (`11010110`) matches `utf8_strlen`'s 2-byte UTF-8 lead-byte check and the following `0x87` (`10000111`) matches its continuation-byte check, so the length-counting loop advances by 2 bytes for what are actually two independent, unrelated raw bytes — undercounting `length()` by one (`3` instead of the correct `4`). The underlying byte *storage* is unaffected and fully correct (confirmed via `unpack`); only the character/length *count* returned by `length()` (and anything else built on `utf8_strlen`) is wrong for this narrow class of byte patterns. Pre-existing in `utf8_strlen` itself, predating D85's fix — D85's own bounded `utf8_strlen_n` variant faithfully preserves the identical interpretation (just NUL-safe/bounded instead of NUL-terminated), so this is not a regression from that fix, just something that became *observable* for the first time now that pack/unpack (D67) and NUL-safety (D85) both work correctly enough to reach `length()` with genuine binary data intact. Root cause is architectural: perlc has no per-`PerlValue` UTF8 flag at all (unlike real Perl, which uses exactly such a flag to decide whether a string's bytes should be interpreted as UTF-8 codepoints or raw octets) — `utf8_strlen`/`utf8_strlen_n` unconditionally apply UTF-8 decoding to every string regardless of origin, which is correct for genuine UTF-8 text (`chr()` of a codepoint > 127, `use utf8` literals) but wrong for raw binary data such as `pack()` output. Fixing this properly needs that missing UTF8-flag concept, plumbed through every string-producing operation to decide whether it applies — a separate, large feature gap, not a targeted fix, and out of scope for D85's NUL-truncation-specific remediation. |
| D91 | `no PRAGMA;` — ANY pragma negation (`no strict;`, `no warnings;`, `no warnings 'CATEGORY';`, etc.), with or without arguments — is a hard parse error, not merely a silently-ignored no-op | **OPEN — LOW-MEDIUM (blocks a common idiom outright, rather than degrading gracefully)** | Found 2026-07-15 while writing D77's regression test: adding `no warnings 'substr';` (to silence real Perl's "substr outside of string" diagnostic in a test that deliberately exercises out-of-range `substr` calls) produced `Parse error: unexpected token 'warnings'`, not the silent ignore CLAUDE.md documents for pragmas generally ("Pragmas that aren't backed by `.pm` files are silently ignored" — confirmed that claim only actually holds for the `use PRAGMA` form). Confirmed the gap is general, not `warnings`-specific or argument-specific: bare `no warnings;` (no argument) fails identically, and so does bare `no strict;` — while the corresponding `use warnings;`/`use strict;`/`use warnings 'CATEGORY';` forms all parse fine (functionally inert beyond that, per the existing D56/pragma-no-op caveat, but at least not a parse error). Root cause not investigated beyond confirming the shape of the gap: the parser's pragma-statement handling appears to only recognize the `use` keyword's grammar production, with no equivalent case wired up for `no`. Worked around in D77's own test by dropping `use warnings` from that file entirely rather than trying to selectively silence one diagnostic category. Not fixed here, out of scope for D77. |
| D86 | `print {EXPR} LIST` / `printf {EXPR} LIST` (the brace-block filehandle form) is misparsed — prints the stringified value of `{EXPR}` as if it were an ordinary hash-literal argument, and writes nothing to the intended filehandle | **OPEN — MEDIUM** | Found 2026-07-14 while writing a D12 regression test. `open(my $fh,'>','/tmp/x.txt') or die; print {$fh} "text\n";` under perlc prints `HASH(0x...)` to STDOUT and leaves the target file empty; real Perl writes `"text\n"` to the file and prints nothing to STDOUT. The plain (non-brace) form, `print $fh "text\n";`, already works correctly — confirmed directly, this is specific to the `{EXPR}` block syntax, which perlc's `print`/`printf` parsing apparently doesn't recognize as a filehandle indicator at all, instead parsing `{$fh}` as a bare anonymous-hash-literal expression argument (hence the `HASH(0x...)` stringification) and treating the whole thing as an ordinary print-to-STDOUT with no filehandle redirection applied. Not fixed here — out of scope for D12 (which is about `wantarray` context propagation, not filehandle-argument parsing); this is a narrower, separate, pre-existing parsing gap. |
| D87 | `wantarray()` never returns `undef` for genuine void context (a bare statement-level sub call with no assignment/return/print) — the compiler's wantarray model is binary (scalar/list) with no void state at all | **OPEN — MEDIUM (architectural)** | Found 2026-07-14 while writing a D12 regression test. `sub ctx { print defined(wantarray)?"defined":"void" } ctx();` (bare statement, void context) prints real Perl's `void`-equivalent (i.e. `!defined(wantarray)` is true) but perlc instead reports a *defined* scalar-context value — confirmed via a minimal repro: `ctx();` as a standalone statement sees `wantarray()` as `""`(scalar-false) rather than `undef`. Root cause: `NK::ExprStmt`'s codegen (`case NK::ExprStmt: freeIfOwned(emitExpr(*n.left)); break;`) does no context setup at all before evaluating a bare-statement call, so it inherits whatever `callCtx_`/`currentSubNeedsWantarray_` state happens to already be set (typically defaulting to the binary "scalar" value, `0`) — there is no third "void" value anywhere in the `callCtx_`/`perl_push_wantarray`/`perl_current_wantarray_ctx` mechanism, which is modeled as strictly 0 (scalar) or 1 (list) throughout `codegen.cpp`/`runtime.c`. Fixing this properly would require threading a genuine three-state context (void/scalar/list) through every wantarray-related call site — a materially larger change than a targeted bug fix, and out of scope for D12 (which is specifically about scalar-vs-list propagation into print/printf arguments, not the pre-existing, broader absence of void-context detection). Not fixed here. |
| D88 | `callCtx_` (the compiler's list-context-propagation flag) leaks list context through scalar-forcing operators (`eq`, `==`, and presumably other comparison/arithmetic/string ops) into a call nested inside them, in contexts *other than* print/printf | **FIXED (2026-07-15)** | Found 2026-07-14 while fixing D12 (and confirmed pre-existing, not introduced by that fix). `my @a = (ctx() eq "x");` — `ctx()` is nested inside an `eq` comparison, whose result (not `ctx()`'s own return value) is what actually gets assigned to `@a`. Real Perl always evaluates `eq`'s operands in scalar context regardless of the outer expression's context, so `ctx()` should see `wantarray()==false`; perlc instead reported `true` (list context leaked from `my @a = (...)`'s list-context RHS straight through the `eq` operator into `ctx()`). Root cause: `callCtx_` is set to `1` before evaluating an entire RHS/argument expression tree at each of its several call sites in `codegen.cpp` (list assignment RHS, lvalue slice RHS, hash-from-list RHS, etc.), but no comparison/arithmetic/string BinOp codegen reset it to `0` before evaluating its own operands — so if a Call was buried under one of those operators rather than being the expression's own top-level node, it incorrectly inherited whatever `callCtx_` happened to be set to further out. **Fix**: rather than auditing and special-casing every individual `callCtx_`-setting call site (the approach D12's narrower print/printf-specific fix took, via `CodeGen::isCallLikeForContext`), found that every non-short-circuit binary operator (arithmetic, string, comparison, bitwise, `x`, `<=>`/`cmp`) funnels through one single, shared spot in `CodeGen::emitBinOp` to evaluate its left/right operands (`&&`/`||`/`//`/`?:` have their own separate branches earlier in the function and are deliberately NOT touched, since e.g. `||`'s right-hand branch, when reached, should transparently inherit the outer context in real Perl — forcing scalar context there would be wrong). Saving `callCtx_`, clearing it to `0` before evaluating each operand, and restoring it afterward at that one choke point fixes the leak for the entire operator class in one place. Verified byte-for-byte against real Perl: the original `eq` repro; every other operator class (`==`, `+`, `.`, `cmp`, `<=>`, `x`); the leak at other `callCtx_`-setting call sites beyond plain list assignment (lvalue array-slice RHS, hash-from-list RHS); a call nested two operators deep (`(ctx() eq "x") && 1`); regression confirming a *direct*, top-level call RHS still correctly sees list context (unaffected by this fix); regression confirming D12's print/printf-specific fix still works (both a direct call argument and one buried under `eq`); and regression confirming ordinary arithmetic/string/comparison results are still computed correctly (this fix only touches context propagation, never the operators' own results). **Four separate, unrelated, pre-existing defects found while writing this fix's regression test, logged as D92-D95, NOT fixed here** (all reproduce identically with and without this fix applied, confirmed via `git stash`): D92 (a runtime crash from a specific combination of block-nested `sub` declarations), D93 (`wantarray() ? (LIST) : SCALAR` ternary-form list return doesn't propagate correctly), D94 (`\|\|`'s right-hand operand doesn't inherit the outer list context), and D95 (`EXPR x N` in array-assignment position is always wrongly treated as list repetition). Tests: `tests/d88_callctx_leak_smoke.pl` (1 check) and `tests/d88_callctx_leak.pl` (19 checks across 7 sections) — verified byte-for-byte against real Perl. Full `tests/harness.sh` regression suite re-run after the fix with zero new failures. |
| D92 | A named `sub` declared inside one bare block, combined with a *different* named `sub` declared inside a *different* bare block in the same file, a shared file-scope array both subs push to, and a `check()`-style helper sub, crashes at runtime | **OPEN — HIGH (crash) but narrow/obscure trigger combination** | Found 2026-07-15 while writing D88's regression test — a broader version of the test crashed with `Segmentation fault`, gdb backtrace: `perl_clone()` calling `strdup()` on a garbage pointer (`0x7200000000`), called directly from `main()` (i.e. at top-level statement execution, not inside any sub's own call frame). Confirmed via `git stash` to reproduce byte-for-byte identically on the pre-D88-fix binary — entirely unrelated to D88's `callCtx_` changes. Isolated a ~20-line minimal repro (see D88's REMEDIATION.md entry for the exact script); confirmed the crash requires the *combination* of: at least two named subs each declared inside their own (different) bare `{ }` block (not at top level — moving both sub declarations to top level, otherwise identical, does NOT crash), a shared file-scope array at least one of those subs pushes to, and a separate `check()`-style helper sub being called after each block. Not root-caused further (each individual ingredient removed independently stopped the crash; the specific interaction wasn't traced into codegen.cpp) — flagged as HIGH severity because it's an actual crash, but explicitly not investigated deeply given how narrow and unusual the trigger combination is (this exact shape wasn't hit by 15 prior fixes' worth of test-writing this session). Worked around in D88's own test by declaring all subs at the top level. |
| D93 | `sub f { return wantarray() ? (LIST) : SCALAR; }` (the idiomatic wantarray-dispatch pattern using a ternary) does not correctly return the LIST branch's list in list context | **OPEN — MEDIUM (common idiom, silently wrong result)** | Found 2026-07-15 while writing D88's regression test. `sub listret { return wantarray() ? (1,2,3) : "scalar"; } my @a = (listret());` gives `@a` as empty/wrong under perlc instead of real Perl's `(1,2,3)`. Confirmed via `git stash` to reproduce identically without D88's fix — pre-existing, unrelated. Confirmed the *ternary* form specifically is the trigger: rewriting the same sub using `if (wantarray()) { return (1,2,3); } else { return "scalar"; }` instead works correctly under perlc. Not root-caused further; likely related to how the ternary's list-valued branch gets wrapped/tagged (`PERL_LIST_RESULT`, per the architecture notes in `CLAUDE.md`) when reached through `?:`'s codegen rather than a plain `if`/`else`'s. Worked around in D88's own test by using the `if`/`else` form throughout. |
| D94 | `\|\|`'s right-hand operand does not inherit the outer expression's list context when reached | **OPEN — LOW-MEDIUM (narrow, opposite-direction sibling of D88)** | Found 2026-07-15 while writing D88's regression test. `sub falsy { return 0; } sub listret { if (wantarray()) { return (7,8,9) } else { return "scalar" } } my @a = (falsy() \|\| listret());` gives `@a` as `("scalar")` under perlc instead of real Perl's `(7,8,9)` — real Perl's `\|\|` transparently passes its own surrounding context through to whichever operand's value actually becomes the expression's result (the left operand is always boolean-tested in scalar context, but the *right* operand, if reached, is evaluated in the context the whole `\|\|` expression itself is in). Confirmed via `git stash` to reproduce identically without D88's fix — this fix's `emitBinOp` change deliberately does not touch the separate `&&`/`\|\|`/`//` branches earlier in the function at all, so this is an independent, pre-existing gap in that branch's own context handling, not something D88's fix should have addressed. The conceptual opposite of D88 itself: D88 was list context incorrectly *leaking through* an operator that should force scalar; D94 is list context incorrectly *failing to pass through* an operator that should transparently forward it. Not fixed here. |
| D95 | `EXPR x N` used as an array-assignment RHS (e.g. `my @a = (f() x 2)`) is always treated as list repetition, even when the left operand isn't actually a parenthesized list literal | **OPEN — LOW-MEDIUM (wrong element count, silent)** | Found 2026-07-15 while writing D88's regression test. `sub ctx { return "x"; } my @a = (ctx() x 2);` gives `@a` as `("x","x")` (2 elements) under perlc; real Perl gives `("xx")` (1 element, the *string* `"x"` repeated) — real Perl only performs *list* repetition when `x`'s left operand is syntactically written as a `(...)`-enclosed list (`(1,2) x 3`, `(f()) x 3`); a bare `f() x 2` with no parens directly around `f()` is always scalar/string repetition, regardless of the surrounding assignment's own list/scalar context. Root cause: `CodeGen::emitArrayPtr`'s `"(LIST) x N — list repetition in array context"` special case (a completely separate code path from `emitBinOp`, taken whenever a `BinOp("x")` node is reached while evaluating an array-context RHS) unconditionally treats *every* `x` BinOp it sees this way, with no check for whether the left operand was actually a parenthesized list at parse time. Confirmed via `git stash` to reproduce identically without D88's fix (this fix never touches `emitArrayPtr`). Worked around in D88's own test by checking the `x` operator's context-propagation behavior via plain scalar assignment (`my $s = ctx() x 2;`) instead of array-assignment position, which is unaffected by this separate bug. Not fixed here. |
| D89 | `die` with a message lacking a trailing newline never appends Perl's standard `" at FILE line N.\n"` suffix | **OPEN — LOW (cosmetic, affects error-message accuracy)** | Found 2026-07-15 while writing D52's regression test. Real Perl: `die "no newline"` (inside `eval` or uncaught at top level) produces `"no newline at FILE line N.\n"` — the interpreter appends the calling file/line whenever the message doesn't already end in `\n` (a message ending in `\n` is left verbatim, real Perl's documented convention for suppressing the location suffix). perlc's `perl_die()` (runtime.c ~3621-3641) does neither: the eval-caught path (`s_eval_depth > 0`) does `perl_assign(&s_dollar_at, msg)` verbatim with no suffix logic at all, and the uncaught top-level path just `fputs`s the raw message to stderr (adding a bare trailing `\n` if missing, but never the `" at FILE line N."` text) before `exit(1)`. Confirmed both paths reproduce identically: `eval { die "no newline" }; print $@;` → perlc `no newline` vs real Perl `no newline at FILE line N.\n`; `die "no newline top level";` (uncaught) → perlc `no newline` vs real Perl `no newline at FILE line N.\n` (and perlc's exit code is `1` vs real Perl's `255` for the uncaught case, though that discrepancy wasn't investigated further here). Fixing this needs `die`'s call site to plumb through the current file/line (already tracked elsewhere in the compiler for `-g` debug metadata and `caller()`) so `perl_die()` can append it exactly when the message doesn't already end in `\n` — not attempted here, out of scope for D52's specific `$@`-as-assignment-target mandate. Not fixed here. |

**`make test` currently FAILS** (contradicts CLAUDE.md's "Build & Test" claim that it passes): `tests/xs_ffi.pl` reports `UNEXPECTED FAILURES: clock_gettime_sec_positive, clock_gettime_realtime_sec_positive, getuid_defined, getgid_defined, getpid_defined, getpid_positive`. The two `clock_gettime_*` failures are a direct consequence of D67 (`unpack("LL",$buf)` never populates `$tv_sec`). The four `get{u,g,pid}` failures are because `tests/xs_ffi.pl` calls a `syscall()` builtin that doesn't exist in perlc at all — no lexer keyword, no runtime function — so it silently parses as a no-op bareword call (the general pattern D36 already documents) and the result variables stay undefined. `syscall()` isn't claimed anywhere in CLAUDE.md/README.md, so this is a missing feature exposed by an existing test, not a regression — but it does mean the top-banner "`make test` ... passes" claim is currently false as written.

### Resolved / Not Applicable

| Defect ID | Problem | Status | Notes |
|-----------|---------|--------|-------|
| D1 / D11 | Compound assignment (`-=`,`*=`,`/=`,`%=`) on shared scalars used the wrong op | **FIXED** | Re-verified directly: all four ops give correct results (90/30/5/2) on a `: shared` scalar across a thread boundary. |
| D2 | `chop @arr` behaved like `chomp @arr` | **FIXED** | Re-verified: `chop @a` removes the last char of every element, matches real Perl exactly. |
| D3 | `local @arr`/`local %hash` no-op for function-scope vars | **FIXED** | Re-verified including restore-on-`die`/`eval`-unwind. |
| D5 | Closure + range-with-captured-variable emitted `undef` bound | **FIXED** | Re-verified with 2 and 3-level nested closures. |
| D6 | `for (my $i = 0; ...)` C-style init claimed dead code | **STALE — was already fixed**, doc not updated | Re-verified 4 variants including pre-declared-var and multi-clause comma forms (`for (my $a=0, my $b=5; $a<3; $a++,$b--)`, added this session) — all correct. |
| D13 | DESTROY handling "unclear if fully functional" | **FIXED / confirmed working** | `tests/destroy.pl` output is byte-identical to real Perl. |
| D15 | `specialVars` list duplicated in codegen.cpp | **FIXED / stale** | Only one definition exists now (codegen.cpp:1617-1618). |
| D16 | `xs_dbi_test.pl` no-op placeholder | **NOT-APPLICABLE** | File no longer exists; superseded by `tests/dbi_sqlite.pl` and `tests/xs_ffi.pl`, both of which have real `check()`-based assertions. |
| D10 | Static counters don't reset between compilations | **NOT-APPLICABLE** | `main.cpp` processes exactly one input file per process invocation (no REPL/multi-file loop exists post-JIT-removal) — counters can't leak across compilations because there is never more than one per process. |
| D18-D21, D7 (original number, retired) | Renumbered/split into D38a/D38b/D38c (substitution bugs) and D8a/D8b (or-RHS bugs) above with concrete repros, replacing the original vague "npos underflow"/"small subset of keywords" descriptions. | — | — |

### This session's fixes (2026-07-08/09, already committed: `b403168`, `fa5961b`)

These were found and fixed while adding C-style `for`-loop comma-operator support (`for ($i=0,$j=10; ...; $i++,$j--)`) — all three were pre-existing and independent of that feature, just newly exposed by it:

- **Unboxed-int `pre--`/`post--` fast path double-negated the decrement** (`cur - (-1)` instead of `cur - 1`) — wrong results and even infinite loops for `$j--` outside the single-item `for`-step fast path. Fixed in `src/codegen.cpp` (~line 4162).
- **`AnonSub` closures never captured `@arrays`/`%hashes`** from an enclosing block, only scalars — `push @log,...` inside a closure over a block-scoped array silently wrote to a detached copy. Fixed by capturing every block-scoped array/hash visible at closure-creation time.
- **`next LABEL`/`last LABEL` silently no-op'd on C-style `for` loops** (only `foreach` registered into `loopLabels_`) — corrupted block control flow. Fixed by registering `for` loops into `loopLabels_` the same way `foreach` does.
- **`make test-tsan` failed to link** — `TSAN_OBJS` in the `Makefile` was missing `ast_tsan.o`/`llvm_early_init_tsan.o` (present in the regular `OBJS`). Fixed; TSan build now clean with zero race reports on `threads.pl`/`threads_atomic.pl`/`destroy.pl`.

---

## Test Coverage Gaps

Re-verified 2026-07-09. Of the original 10 "claimed but untested" items, **9 are confirmed correctly implemented** (smoke + deep tests still need writing, per the testing policy below) and **1 (Carp) is confirmed broken** — see D35/D36 above.

| Feature | Status | Test Needed |
|---------|--------|-------------|
| `@{$href}{LIST}` hash-ref slices | Confirmed working | Smoke + deep test |
| `@{$aref}[LIST]` array-ref slices | Confirmed working | Smoke + deep test |
| `scalar(@{$ref})` | Confirmed working | Smoke + deep test |
| `$h{k}++` on missing keys | Confirmed working | Smoke + deep test |
| lvalue slices `@arr[i,j] = list` / `@h{qw(a b)} = list` | Confirmed working | Smoke + deep test |
| `map { @$_ } @aoa` flattening | Confirmed working | Smoke + deep test |
| `next`/`last` with labels | Confirmed working (`for`/`foreach`/`while`, all mixed-nesting combos) | Smoke + deep test (extend `modifiers.pl`) |
| `$Pkg::arr` / `$Pkg::hash` cross-package | Confirmed working | Smoke + deep test |
| `use parent -norequire` | Confirmed working | Smoke + deep test (extend `inherit.pl`) |
| Carp module (`croak`/`carp`/`confess`/`cluck`) | **Confirmed BROKEN** — see D35, D36 | Blocked on the fix; write regression test alongside it |

### Testing policy going forward

Per project direction: every fix should ship with (1) a **smoke test** that just confirms the feature/fix exists and does the basic right thing, and (2) a **deep test** that explores edge cases and interactions as comprehensively as practical — both verified byte-for-byte against real Perl output via `tests/harness.sh`, not just self-checking assertions in isolation. See `tests/comma_operator_smoke.pl` / `tests/comma_operator.pl` (added 2026-07-08) for the established pattern.

---

## Recommended Development Workflow

**Harness-as-Gate Policy**: `make test-all` is the mandatory pre-commit gate. Run it before every commit.

```bash
# 1. Build the compiler
make clean && make

# 2. Run ALL correctness tests (harness gate — mandatory)
make test-all

# 3. If benchmarks are cached, results are reused. If not, they are recorded.
#    To force re-run of benchmarks after a significant change:
make test-all -- --force-benchmark

# 4. To view cached benchmark results:
make test-cache

# 5. To clear cache and force fresh runs:
make test-clear-cache

# 6. Safety gates before merging
make test-valgrind    # memory safety
make test-tsan        # data race detection (threads/destroy)
make test-tsan-full   # broader TSan coverage
```

### When to Use `--force-benchmark`
- After fixing a bug that could affect benchmark correctness
- After optimizing code that changes algorithmic behavior
- When adding new features that might impact performance
- When the cache is stale (e.g., after hardware changes)

### When Cache Is Sufficient
- Small fixes to unrelated code paths
- Bug fixes that don't affect numerical output
- Documentation/comment changes
- Build system changes (verify with `make test` first)
